@AGENTS.md

# Pro ERP

Multi-tenant SaaS ERP — built with Next.js (App Router), Tailwind CSS, and Shadcn UI, deployable on Vercel. The backend is **Neon (serverless Postgres) via Drizzle ORM**: one shared database, every tenant-scoped table carries an `org_id` column. Attachments go to **Vercel Blob**. (Until 2026-09-16 this was a Google-Sheets-as-database / Google-Drive-as-storage app where each org pasted its own Sheet URLs — that architecture was fully migrated away and removed; see "The Postgres migration" below before assuming any doc, comment, or memory that predates it is still accurate.) Full setup instructions and architecture notes live in [README.md](README.md) — read that before making changes, but cross-check it against this file for anything storage-related, since the README may still describe the pre-migration Sheets setup until someone updates it.

## Status

### The Postgres migration (2026-09-16 to 2026-09-19) — read this first

The entire persistence layer was rewritten from Google Sheets to Neon Postgres, in six phases, each verified against live data before the next started, then closed out with a backend audit, a frontend audit, and an independent two-agent security review. This was a full replacement, not a wrapper — `googleSheets.ts`, `tenantSheets.ts`, `moduleSheets.ts`, `sheetUrl.ts`, `googleAuth.ts`, `googleDrive.ts`, and `platform/provisioning.ts` are all **deleted**, along with the `googleapis` dependency, the `/onboarding` wizard, and every "paste a Sheet URL" / "connect a Drive folder" screen.

- **Schema**: `src/db/schema/**`, one file per domain cluster (`platform`, `auth`, `settings`, `tasks`, `inward`, `inventory`, `ppc`, `fms`, `parties`). Every generated business ID (`VEN-xxxx`, `RUN-xxxx`, `ORG-xxxx`, ...) is the real Postgres primary key, verbatim — no surrogate integer/UUID keys were added, since these IDs were already load-bearing as string foreign keys everywhere (FMS's `Context_Ref` convention, `Form_Data`'s lookup-field `__id` stash, `BOM_ID`/`Template_ID`/`Instance_ID` grouping keys).
- **Generic data layer**: `src/db/repo.ts` — `listByOrg`/`findById`/`insertRecord`/`updateById`/`deleteById`, every one of them requiring `orgId` as a real argument, type-constrained to only work against tables with a plain `id` primary key. Tables with a **composite** primary key (`fms_templates` — `template_id, step_no`; `bom` — `bom_id, line_no`; `plan_materials` — `plan_id, sku`; `holiday_list` / `fms_weekoff_overrides`'s date-scoped queries) don't satisfy that type and get direct, bespoke Drizzle queries in their own domain file instead — **any query against one of these must still carry its own `eq(table.orgId, orgId))`, since `repo.ts` isn't there to enforce it for them.**
- **Migration tooling (switched 2026-09-21): real migration files now, not `drizzle-kit push`.** `drizzle/0000_mute_white_tiger.sql` is a baseline migration generated from the schema as it stood at cutover — it was never actually executed against the live DB (all 29 tables already existed); instead it was marked applied by hand-inserting its hash/timestamp into `drizzle.__drizzle_migrations` (the exact table/hash/timestamp scheme `drizzle-orm/neon-http/migrator`'s `migrate()` itself uses — read `node_modules/drizzle-orm/migrator.js` and `node_modules/drizzle-orm/neon-http/migrator.js` if this ever needs redoing), so `drizzle-kit migrate` correctly sees it as already-applied and no-ops on it. **From now on**: change `src/db/schema/**`, run `npx drizzle-kit generate` (writes a new numbered `.sql` file under `drizzle/` — review it before applying, this is the rollback/audit story `push` never had), then `npx drizzle-kit migrate` to actually apply it to the live DB. **Never run `npx drizzle-kit push` again** — besides being superseded, it's also the command that produced a real near-miss on 2026-09-21 (a mis-scoped `--tablesFilter` made it briefly plan to recreate the entire schema; caught before any damage, but see the `settings_org_id_key_unique` working note below for the underlying quirk that triggered it).
- **Caching removed**: `tenantCached`/`cached` (30s/60s TTLs that existed purely to survive Sheets' cost) are gone from every migrated path — a real indexed Postgres read replaced them, which also fixed several up-to-30-seconds-stale-data bugs the caching had introduced (a revoked report-share link, a just-deactivated user, a just-created org — all now take effect immediately, not "within 30 seconds depending which serverless instance you hit"). `src/lib/cache.ts` itself is untouched/unused now — nothing imports it.
- **Signup is simpler now**: no Sheet URL, no `verifySheetAccess` probe — just org name / admin name / email / password. `organizations.systemSheetId` was a real column through most of the migration (kept for continuity, always written as `""`) and was dropped outright once nothing read it.
- **Attachments are Blob-only now, unconditionally** — the old Drive-folder-connect option (`src/lib/googleDrive.ts`, `verifyDriveFolderWritable`, the "personal Drive has no quota" fallback dance) is gone. `src/lib/storage.ts`'s `uploadAttachment()` is a single unconditional call into Vercel Blob. `GOOGLE_SERVICE_ACCOUNT_EMAIL`/`GOOGLE_PRIVATE_KEY` are no longer read by any code path in the app — harmless to leave set in Vercel/`.env.local`, safe to remove whenever convenient.
- **Real bugs found and fixed during the migration's own audit passes** (worth remembering as patterns, see "Working notes" below for the durable lessons):
  1. `deleteOrganization()` only cascaded into 4 of 22 tenant-owned tables when first ported — deleting any org that had touched Tasks/Inventory/FMS/etc. threw a raw FK-violation instead of deleting. Fixed to cascade into all of them, atomically via `db.batch()`.
  2. The platform delete-organization confirmation dialog told the operator the org's data was untouched/safe — true under the old Sheets architecture, **false** now (deletion is a real, permanent wipe of every table). Copy corrected.
  3. `GET /api/fms/steps/[runId]` let any signed-in user in an org read *another* user's step context — including "Existing FMS" reference data such as a vendor's bank details — without being that step's assignee or holding `FMS_ADMIN`, even though the sibling complete-step endpoint already enforced exactly that check. Fixed.
  4. `plans.ts`'s local `hasFmsLine()` (a Sheets-era copy kept to avoid a real circular import with the FMS engine) would have silently always returned `false` post-migration — meaning `completePlan()` would double-write FG stock even when an FMS Line already wrote it via its own Action. Moved the real check into `fms/engine.ts` (which now owns `fms_runs`), `plans.ts` calls it via a dynamic `import()` to keep breaking the same cycle.
- **`DATABASE_URL` is now a required env var** (Neon's pooled connection string) — the app cannot boot any page without it, including login. Set on Vercel already; confirm it's set before ever pushing a change that touches this area to `main`, or production goes down immediately (every page needs the DB).

### New FMS capabilities added after the migration (2026-09-19)

- **Per-flow nav + Flow Board**: every Active FMS template gets its own nav item (gated to whoever has a step in it, or `FMS_ADMIN`) opening `/fms/[templateId]` — a dedicated board (one row per running instance, click for a stepper detail dialog) instead of everything mixed into the generic `/fms` tabs.
- **Linked "Lookup" form field**: an Admin can point a Form field at any connected module (e.g. Customers) and map other fields on the same step to autofill from the selected row — generic across modules, not hardcoded to any one of them. Runtime: `src/app/fms/complete-step-dialog.tsx`'s `LookupCombobox`; config: `FormFieldLookup` in `src/lib/fms/dataSource.ts`.
- **Admin-only full FMS reset**: a "Saare FMS Templates + Runs Reset Karein" button on the Templates board, gated on `role === "Admin"` (both client-side and by the API route via `requireRole`), requires typing "DELETE" to confirm — wipes every template and run for the org. For clearing test data built while designing flows, before real use begins.
- **TAT can now be in Minutes**, not just Hours/Days (`FmsTatUnit = "Minutes" | "Hours" | "Days"`) — the working-hours calendar math already worked in minutes internally, so this only needed the new unit threaded through `calendar.ts` and the builder UI.
- **Per-step WhatsApp notification list**: a step can name zero, one, or several users (independent of who the next step's own run goes to) who get a WhatsApp message the instant that step is marked complete. `fms_templates.notify_on_complete` (`text[]`); sent from `engine.ts`'s `notifyStepComplete()`, best-effort exactly like `emitFmsEvent`'s chaining — never blocks or fails step completion, every recipient sent in parallel, a recipient with no phone on file skipped silently, a real send failure logged.
- **A step's deadline can be sourced from an earlier step's own field, not just a fixed TAT** — tick "Deadline pichle step ke field se nikaale" in the template builder, pick the source step + field, and give an offset (+/-). This is what lets a single "Lead Days" number typed once drive multiple downstream deadlines (see Purchase FMS below).

### This session's build-out (2026-09-19 to 2026-09-21) — this is the "parent" section: keep it current

Everything below shipped in one long continuous session, each feature committed and pushed once the user confirmed it worked. **This section is the living master plan the user asked for — update it (don't replace it) every time a feature lands, rather than letting "Next up" silently go stale.**

- **Vendor ↔ Item linking** (`vendor_items` table, `src/lib/parties/vendorItems.ts`): a vendor-item pair carries its own lead time and unit price; `listVendorsForSku`/`listVendorsForSkus` sort cheapest-priced first (unpriced vendors sort last) and feed both the Reorder page's vendor suggestions and the Purchase FMS's PO Issue screen. The vendor-creation form is deliberately labelled "Purchase Vendor" (not just "Vendor") because OEM/Manufacturing vendor types may be added later and would need their own form.
- **Nav reorganized into dropdown groups** (`src/components/nav-links.tsx`'s `NavGroup`, assembled per-viewer in `src/components/app-shell.tsx`): **MDO** (Tasks/Flow/Leave/Reports/Performance), **PMS** (BOM/PPC/every FMS Line a plan actually starts), **Stock** (Inventory/Finished Goods), **FMS** (Inward/Purchase/every other flow), **Others** (Vendors-Customers/Users/Settings/Platform). A PPC-connected Line is told apart from an ordinary FMS flow by `triggerEvent === "PRODUCTION_STARTED"` OR actually being referenced by a plan (`listUsedFmsTemplateIds()`) — a Line's own default trigger is `MANUAL`, so the trigger alone can't be trusted.
- **UI motion pass** (applied via the `ui-ux-pro-max` skill + 21st MCP, "Standard" intensity): hover micro-interactions, scroll reveal, stagger lists across shared UI and key pages, without touching the existing color system, fonts, or accessibility behavior.
- **FMS TAT unit bug fixed**: `src/app/api/fms/templates/schema.ts` only allowed `Hours`/`Days` in its zod schema, silently rejecting the "Minutes" option the builder UI already offered. Added `"Minutes"` to the enum.
- **Purchase FMS — built as a hardcoded flow, not a generic FMS Template** (`src/db/schema/purchase.ts`, `src/lib/purchase/`, `src/app/purchase/`): Indent Approve (existing Indents flow) → **PO Issue** (vendor-first: pick a vendor, every Approved indent it supplies gets pre-suggested with old/new price, one PO can bundle several indents, PO attachment required) → **Follow Up** (a real actionable step, deadline = vendor's own lead time minus 1 day) → **Material Received** (invoice + per-item partial/full quantity, reusing the existing `receiveIndent` logic so stock updates identically either way). A new **Purchase Setup** admin screen (`/admin/settings`) sets each step's Doer + TAT (steps 3/4 need only a Doer — their deadline derives from the vendor's lead time). This satisfied the "Next up" Purchase FMS item below, but as bespoke screens rather than the originally-envisioned generic-FMS-Template configuration — reflects better because PO bundling/pricing needed UI the generic Form/Lookup model doesn't have a slot for.
- **Finished Goods split into its own Inventory view** (`/inventory/fg`, `InventoryBoard`'s new `scope: "goods" | "finished"` prop): one underlying stock model, but the main `/inventory` view now excludes `Category === "FG"` and the FG page shows only FG (and pre-selects "FG" on New Item). Both `completePlan()`'s stock-in and an FMS Production Line's final Action land here automatically — nothing else changed about how FG stock is written.
- **WhatsApp delivery bug fixed**: messages were silently failing (FMS step-complete notify, Task-completion confirmation) whenever a user's phone number lacked the "91" country-code prefix ChatXFlow's API requires. Fixed centrally in `sendWhatsAppMessage()` (`src/lib/chatxflow.ts`) via a `normalizePhone()` helper — a bare 10-digit Indian mobile gets "91" prepended, anything already prefixed or otherwise formatted is left alone.
- **Recurring Task generator's Sunday/holiday bug fixed** (`src/lib/recurringGenerator.ts`, fully rewritten): it previously only skipped explicit Holiday List dates, never the weekly-off day, so a Daily task assigned to run on a Sunday still fired. Now: **Daily** rules simply skip a non-working day outright; **Weekly/15D/Monthly/Quarterly/Yearly** rules carry a missed cycle forward to the next working day instead of losing it (via a per-frequency lookback window, `findMostRecentScheduledDay()`).
- **Holiday List admin CRUD added** (`src/lib/holidays.ts` rewritten from read-only; `src/app/admin/settings/holiday-list-form.tsx`, `src/app/api/admin/settings/holidays/**`): manual add, per-row edit and delete, and CSV/Excel bulk import (same download-template → fill → upload pattern as Items/Vendors/Customers). This list is *in addition to* the weekly-off day (set separately in FMS Shifts) — it exists for one-off extra non-working dates like festivals.
- **Leave System ("buddy system") built end to end** (`src/db/schema/leave.ts`, `src/lib/leave/{approvalSetup,leaves,reassignment}.ts`, `src/app/leave/**`, `src/app/admin/settings/leave-approval-setup-form.tsx`): a Doer files leave and picks their own Buddy; approval runs through a per-org configurable chain (`REPORTING_MANAGER` — resolved per-requester from `users.reporting_manager_id`, set at user-creation time — and/or one-or-more fixed `SPECIFIC_USER` steps like HR/MD); once every step approves (or the chain is empty), and the leave's date range includes today, **every currently-open Task and FMS step for the Doer reassigns to the Buddy** (`leave_reassignments` keeps a full audit trail so reversion is precise); ending the leave (or cancelling it) reverts only what is *still* Pending and *still* held by the Buddy — anything the Buddy already finished stays finished under the Buddy's name. Emergency Leave lets whoever holds the new `LEAVE_HR` module grant file on a Doer's behalf. **Deliberately deferred for v1, by the user's own explicit choice**: no leave balance/quota tracking (approval + reassignment only), and Payroll is a separate, later, not-yet-scoped conversation. Both `processLeaveTransitions()` (activate/revert) and the existing recurring-task generator now run together from one daily cron entrypoint (`runDailyJobs()` in `src/app/api/cron/generate-recurring-tasks/route.ts`).
- **Guidebook (`src/lib/guide.ts` / `guide.en.ts`) substantially expanded** to cover every feature above in plain, non-technical language, including new chapters for Parties (Vendor & Customer Master), Purchase, and Leave, plus new sections for the FG inventory split, the Holiday List screen, and the grouped nav bar — written for a layman reader, matching the existing chapter/section id parity `npm run i18n:check` enforces (79/79 as of this update).

### Modules (business features — mostly unaffected by the storage migration; described here in terms of what they do, not which database they used to use)

- **Module 1 — Auth**: custom email/password login, JWT session in an httpOnly cookie, role-based route guards (`src/proxy.ts`, `src/lib/auth/`). Password hashing (bcrypt), session payload shape, and every guard in `guard.ts` are unchanged by the migration — they only ever touched the JWT, never persistence directly.
- **Module 2 — Admin**: user management (`/admin/users` — create, edit role/status, reset password). `/admin/settings` now shows only genuine app configuration (Logo, WhatsApp/ChatXFlow, FMS Shifts, Week-off Overrides, Inward IQC TAT) — the old per-module "paste a Sheet URL" list is gone, since every module's table already exists in the shared schema.
- **Module 3 — Task Delegation**: one-time task assignment (`/tasks`) with Priority, Completion date & time, optional attachment, optional completion-proof upload (now via Blob unconditionally).
- **Module 4 — Dashboard & MIS**: per-user (`/dashboard`) and team-wide (`/performance`) MIS scoring (Done on Time / Delay Done / Not Done), computed dynamically from timestamps — nothing is a stored status flipped by a background job.
- **Module 5 — WhatsApp (ChatXFlow)**: token/phone/base-URL configured from Settings (per-org, in the `settings` table), completion-confirmation + pending-reminder automations for Tasks, daily Vercel Cron (`vercel.json`). FMS gained its own, separate WhatsApp use (per-step notify list, above) on the same `sendWhatsAppMessage()` primitive.
- **Module 6 — Inward FMS & IQC**: inward entry form (`/inward`), IQC quality-check modal, routing to Failure Log / IMS Inward on save. Working-hours-aware TAT/deadline (`IQC_TAT_Value`/`_Unit`/`_Deadline`, Admin-configurable in Settings), computed against a company-default shift since an inward entry has no single assignee to peg a shift to. **`Party_Name` is now wired to Vendor Master (2026-09-21)** — `VendorPicker` (`src/components/vendor-picker.tsx`) type-ahead suggests Active vendors from a new session-only `/api/parties/vendors/lookup` (id+name only, no bank/GSTIN — mirrors `/api/inventory/lookup`'s reasoning that recording an entry shouldn't require the full `PARTY_MASTER` grant); picking one sets the new `inward_iqc_fms.vendor_id` column alongside `Party_Name`. Deliberately still free text, not a forced Lookup: an inward entry can name a party that isn't in Vendor Master yet, so `vendor_id` is additive (empty when the typed name has no match) rather than blocking the entry.
- **Recurring Task Engine**: daily cron (`src/lib/recurringGenerator.ts`) generates dated occurrences per rule (D/W/15D/M/Q/Y), holiday-aware for every frequency, month-end-safe. A subtle date-handling bug was caught and fixed during the migration: `Due_Date`/`Assign_Date` must round-trip as IST wall-clock strings (`toIstWallStamp()`/`istDayKey()`), not plain UTC `.toISOString()`, because this generator does a raw `startsWith(todayIST())` string-prefix check — a naive ISO string would silently roll onto the wrong calendar day for anyone due between IST midnight and 5:30am.
- **Module 11 — Analytics dashboard** (`/dashboard` → Dashboard tab): per-module bar/donut/timeline charts, gated by the viewer's grants, with date-range filters in the URL so a view is shareable. Hand-rolled SVG in `src/components/charts.tsx`, no charting library. **No longer wrapped in `tenantCached`** — Postgres reads are fast enough that the 30s staleness this used to accept isn't needed; each read still degrades to `null` independently, so one broken module never blanks the rest.
- **MIS scoring is a penalty scale**: 0% is spotless, −100% the worst. On time costs nothing, late costs 0.5, missed costs 1, divided by tasks evaluated. `formatScore()`/`getScoreColorClass()` are the only display paths.
- **Module 10 — Organization logo**: optional at signup, changeable from Settings. Always Blob (never was Drive, unaffected by the migration) — renders on every page load and needs a CDN without an auth round-trip. Raster only.
- **Module 9 — Guidebook** (`/guide`, `src/lib/guide.ts`): the in-app manual, written as data, filtered per viewer by `guideFor()`. The onboarding/Sheet-connection sections were removed during the migration; add content by editing `GUIDE`, not by writing a page.
- **Module 8 — Recurring rule pause, quality records, platform console**: recurring rules can be paused/resumed from `/tasks`; Failure Log and IMS Inward are readable at `/inward`; `/platform` lists every organization and can suspend/delete one.
- **Module 8 — Per-user module access**: `Module_Access` (now a real `text[]` column on `users`, was a comma-separated string) + `src/lib/moduleAccess.ts`. Role is the privilege level; grants decide which modules a person works in. Baked into the JWT at login — `requireModule()` costs no DB read.
- **Module 7 — Multi-tenancy (SaaS)**: self-serve org signup (`/signup`, no onboarding wizard anymore). `organizations` + `users_index` are tables in the same shared Postgres database (used to be a separate "platform registry" spreadsheet Pro ERP itself owned) — login's first hop resolves an org from the email via `users_index`.
- **Module 12 — FMS (Flow Management System)**: a generic engine so a multi-step business process is built by an Admin as data (`/fms` → Templates), not code. `fms_templates` (one row per step, composite PK, mirrors the old flat-sheet shape on purpose), `fms_runs` (one row per step execution), `fms_weekoff_overrides`.
  - **Working-hours calendar** (`src/lib/fms/calendar.ts` + `workingCalendar.ts`, pure/zero-import): a TAT (now Minutes, Hours, *or* Days) counts only real working time — per-user `Shift`, lunch/tea excluded; weekly-off with date+department/user overrides; one open TAT per person, snapped forward through the calendar, never naively added on top of "now".
  - **Data Source is two independent, combinable pieces** (`src/lib/fms/dataSource.ts`): a Form, and/or columns live-pulled from any connected module or an earlier step of the same flow (`THIS_FLOW_SOURCE`) — plus, added after the migration, a **Lookup** field type (see above). `Data_Source_Type` is derived server-side, never trusted from the client.
  - **Action** (Stock Ledger Movement, `src/lib/fms/actions.ts` + `actionRunner.ts`) binds SKU/Qty/UOM from a dropdown of the step's own real field names. Unlike chaining, an Action's write must succeed before the step can complete — a bad SKU or insufficient stock throws and the step stays Pending.
  - **`emitFmsEvent()` is the one chaining primitive** — a completed FMS step, Inward (`INWARD_ENTRY_CREATED`), PPC (`PRODUCTION_STARTED`), and Indent approval (`INDENT_APPROVED`) all call it. Best-effort — a broken downstream flow must never undo something already saved. `notifyStepComplete()` (the new WhatsApp feature) follows the exact same best-effort convention, for the same reason.
  - **Production creates real stock.** `completePlan()` writes an FG stock-In for the quantity actually made, *unless* an FMS Line is already tracking the plan (`hasFmsLine()`, now living in `engine.ts`) — then the Line's own last-step Action writes it instead, at whatever quantity actually survived every stage, so production is never double-counted. **A plan's FG stock has exactly one writer at a time — never let both fire.**
  - **A production plan can name a specific Line** (`fmsTemplateId` on the plan) — `Start Production` then starts only that one template directly (`startFmsInstance`). **A plan with no Line chosen starts no Line at all (fixed 2026-09-21)** — this used to fall back to broadcasting `PRODUCTION_STARTED` to every Active template with that trigger, which meant two products each needing a different Line would both fire for any plan that hadn't picked one; that fallback is now removed, so `emitFmsEvent("PRODUCTION_STARTED", ...)` is no longer called from anywhere. The Line picker (`/api/ppc/production-lines`) deliberately offers every Active template regardless of its own `Trigger_Event`, since a Line's default trigger is `MANUAL` and nobody reliably retypes it.
  - **Templates are versioned like BOM**: editing (`updateFmsTemplate`) mints a new `Template_ID` and archives the old one rather than mutating rows in place — a running instance keeps resolving against the old, still-readable version.
  - A manual **"Start"** button on the Templates board is the only way to run a `MANUAL`-trigger template; every other trigger fires via `emitFmsEvent()`.
- **Module 13 — Vendor & Customer Master** (built 2026-09-16, `/parties`): manual "+ Add" dialogs plus CSV/Excel bulk-import (download template → fill → upload), mirroring the Items-master pattern. Both warn — without blocking — on a duplicate name (case/space-insensitive, checked against existing rows and within the same import file). Now also carries the Vendor↔Item link (lead time + unit price per pair) that Purchase and Reorder both read. This is the foundation the roadmap's Purchase/Sales/Accounts chains all still need; Inward's `Party_Name` is not yet wired to it (see Module 6 above).
- **Module 14 — Purchase** (built 2026-09-20/21, `/purchase`): the hardcoded Indent Approve → PO Issue → Follow Up → Material Received flow described above. `PURCHASE_FMS` module grant covers steps 2-4; step 1 (Indent Approve) uses the existing `INDENT_APPROVE` grant. Doer/TAT for each step comes from the "Purchase Setup" admin screen.
- **Module 15 — Leave (buddy system)** (built 2026-09-20/21, `/leave`): filing, multi-step approval, and Task/FMS buddy reassignment as described above. `LEAVE_HR` module grant is the only leave-specific grant — filing and approving your own chain's steps need no grant at all, since every signed-in user can potentially be a requester or a resolved approver.
- **Bug fixed 2026-09-21 — a BOM's own product never got an Items-master row.** `createBom()` only ever wrote the product's SKU onto the `bom` rows themselves — nothing created a matching `items` row, so the very first time `completePlan()` or an FMS Production Line's final Action tried to record that product's FG stock, it failed with "Items master me nahi hai / SKU nahi mila", even though the product had obviously already been planned and built via BOM. `createBom()` now auto-creates the Item (Category `FG`, UOM `PCS`) the first time a product's BOM is saved, if no Item already exists for that SKU — a second BOM version, or a product whose Item was already created by hand (e.g. as `Semi-FG`), is left untouched. Once the Item exists it shows up in Inventory/Finished Goods with Free/On Hand/ADC/ROP live like any other item; planning fields (Lead Time, Safety Factor, MOQ, Max Level) are still left blank for the Admin to fill in, same as any manually created item. `actionRunner.ts`'s own "Items master me nahi hai" error message was also reworded to match `completePlan()`'s (tell the user exactly what to do), while investigating the live report that surfaced this bug.
- **Inward's `Party_Name` wired to Vendor Master (2026-09-21)** — see Module 6 above for the shape (`VendorPicker`, `/api/parties/vendors/lookup`, `inward_iqc_fms.vendor_id`). Closes the item that was open in "Next up" since the Postgres migration.
- **Fixed 2026-09-21 — a production plan with no chosen Line no longer broadcast-starts every `PRODUCTION_STARTED` template.** Found while the user was verifying their first real Production Line ("Production Line 1") end to end: `src/app/api/ppc/plans/[planId]/route.ts`'s "start" action used to call `emitFmsEvent("PRODUCTION_STARTED", ...)` whenever a plan's `fmsTemplateId` was blank, which fires *every* Active template with that trigger — exactly the "two products, two Lines, one plan accidentally starts both" ambiguity that picking a Line was supposed to remove, just for the one case (no Line picked) nobody had closed off yet. Removed outright, by explicit user decision: a blank `fmsTemplateId` now starts no Line at all, full stop. `emitFmsEvent("PRODUCTION_STARTED", ...)` is consequently no longer called anywhere in the codebase (the function itself is untouched and still used for `INWARD_ENTRY_CREATED`/`INDENT_APPROVED`/step-to-step chaining).
- **Switched from `drizzle-kit push` to real migration files (2026-09-21)** — see "Migration tooling" above for the mechanics. `drizzle/0000_mute_white_tiger.sql` is the baseline (the schema as it stood at cutover, marked applied without being re-run, since every table already existed); every schema change from here on gets its own generated, committed, reviewable `.sql` file.
- **Module 16 — Lead FMS (built 2026-09-21, `/leads`)**: the first leg of the Sales chain — Lead capture (manual punch or bulk Excel/CSV import, template download, same warn-not-block-on-duplicate convention as Vendors/Items) → pipeline (`Qualify` → `Follow_Up`, loops on "Call Back Later" → `Meeting_Scheduled`, loops on "Reschedule" → `Negotiation` → `Quotation_Sent` → `Order_Confirmed`/`Lost` from anywhere). Every transition appends a `lead_activities` row — a lead is read as a timeline, not a single status cell, same convention as `leave_reassignments`. Built as its own hardcoded flow, not a generic FMS Template — same reasoning as Purchase (a pipeline board, bulk import, and a quotation grid all need bespoke UI the generic engine has no slot for). `LEAD_FMS` is the one module grant covering the whole flow.
  - **Quotation** (`src/lib/leads/quotations.ts`, `quotationMath.ts`, `quotationPdf.tsx`): mechanism adapted from a reference CRM the user handed over to learn from (their own separate project, not merged in) — a whitelist-grammar formula calculator for line quantities (e.g. typing `2.5*3+1.2` into a qty cell; hand-written recursive-descent parser, deliberately not `eval`/`new Function` since the string reaches the server), GST charged on goods-plus-freight, and a PDF built with `@react-pdf/renderer` (no headless-Chromium dependency — worth reusing for any future printed document, e.g. a real PO document generator) uploaded via the existing Blob attachment path (`uploadAttachment()`). Money stays plain `numeric` throughout — the reference's own integer-paise/integer-milli-qty scheme was deliberately NOT adopted, to stay consistent with how every other module in this codebase (Purchase, vendor_items) already represents money. `quotations` carries a real `(org_id, quotation_no)` unique constraint; `quotationNo` is allocated by reading the current highest number and retrying on a genuine collision — same shape as Purchase's PO numbering.
  - **Deliberately stops at "Order Confirmed."** Accepting a quotation only flips the lead's status and best-effort emits `emitFmsEvent("LEAD_ORDER_CONFIRMED", "LEADS:<leadId>")` — no `Order`/`Company`/`Contact` record is created. That is the next, separate module (see "Next up" below) to build against this exact event, the same way Purchase's PO Issue reacts to `INDENT_APPROVED`.
  - **"Quotation Setup"** (new admin screen, mirrors "Purchase Setup"): company letterhead, bank details, default subject/note/terms, GST% default, quotation-number prefix/start, validity days — all via the existing `settings` key-value store. Reuses the org's existing logo (`ORG_LOGO_URL`, Module 10) rather than a second upload.
  - **Walk-in quotations are done (2026-09-22)** — a "+ Nayi Quotation" action on `/leads`' own Quotations tab opens a dialog that either picks an **Existing Customer** (scoped to Customer Master rows *this salesperson themselves created* — `customers.createdBy`, via `/api/leads/customers-lookup`, needing only `LEAD_FMS` not `PARTY_MASTER`) or adds a **New Customer** on the spot (writes to the real Customer Master via the existing `createCustomer()`, then quotes it immediately). `createWalkInQuotation()` and the lead-driven `createQuotation()` now share one `insertQuotationRow()` so both agree on numbering/retry/Setup-defaults. Terms & Conditions has always been independently editable per quotation on the builder page (`quotation-builder.tsx`'s own Textarea, saved via the header PATCH) on top of Quotation Setup's org-wide default — confirmed working end to end via a live test after the user asked, not a new feature.
- **Module 17 — Order FMS (built 2026-09-22, `/orders`)**: leg 2 of the Sales chain. Two entry paths converge on one pipeline — **Lead-sourced** (an Accepted quotation with `order_id = ''` sits in Order FMS's own intake queue, mirroring Purchase's own Approved-indent candidate-finding; opening one maps every free-text `quotation_items` line to a real Item and confirms/creates the real Customer Master row behind it — a lead-driven quotation was never required to point at one — in a single atomic action, landing directly in `Payment_Review`) and **Direct** (a salesperson's own Order Form, real Items from the start via the existing `ItemPicker`, same Existing/New Customer picker as walk-in quotations, optional PO upload). **`Items_Pending` is a real schema enum value that is deliberately never actually persisted** — completing Step 1 is all-or-nothing (an order with unmapped lines has no real SKU to reserve against anyway), so no DB row ever sits in that status; a future session touching this should not assume it does.
  - **`Payment_Review`'s credit gate** (`workPaymentReview()`/`computeCreditPosition()` in `src/lib/orders/orders.ts`): a customer with no `creditLimit`/`creditDays` needs a real advance (`order_payments` sum `> 0`) before proceeding. A customer with credit gets both checks together — would this push their total outstanding (recomputed fresh every call, every non-Cancelled order's `orderValue` minus its own payments, summed — never a stored running total) over `creditLimit`, or does any of their orders sit unpaid past its own `createdAt + creditDays` — either trips `Credit_Hold`. Clearing one is a deliberate human action (`approveCreditHold()`), authorized to the org's configured **Credit-Hold Approver** (a specific person, set in the new "Order Setup" admin screen — deliberately NOT a module grant, per the user's explicit instruction that this varies by org and should be assignable the same place Doer/TAT already are) or an Admin override.
  - **`Stock_Check` adds a fourth stock-commitment source.** `positionFor()`/`buildItemStock()` (`src/lib/inventory/ledger.ts`) now take an `orderReserved` map alongside `committed` (raw material) and `inTransit`: `free = onHand − committed − orderReserved`. `orderReservedBySku()` (`src/lib/orders/orders.ts`) mirrors `plans.ts`'s own `committedBySku()` one level up the chain — sums `order_items.reservedQty` across orders in `Stock_Check`/`Dispatch_Pending`/`Ready_For_PDI`. Reserving processes one order's lines against currently-Free stock (`reservedQty = min(qty, free)`, `shortageQty = qty − reservedQty`) — **never writes `stock_ledger`**, a reservation is not a movement, same as a production plan's own reservation before `completePlan()` actually consumes it. A short line fires both a Task and a best-effort WhatsApp (`notifyShortage()`) to every `PPC_PLAN` holder (a new `listUsersWithGrant()` helper, since none existed — Admins implicitly included, same logic `effectiveModuleAccess()` already encodes).
  - **`Dispatch_Pending` → `Ready_For_PDI`** sets a commit date and best-effort emits `emitFmsEvent("ORDER_READY_FOR_PDI", "ORDERS:<orderId>")` — the seam PDI (leg 3, Module 18 below) picks up from, same pattern as `LEAD_ORDER_CONFIRMED`.
  - `order_payments` is an append-only payment log, not full Accounts (no GL, no invoices) — just enough real structure for the credit gate to work off actual data instead of one "advance amount" guess; a future Accounts module should build on these same rows rather than duplicating them.
  - A new customer added inline during order entry starts with no `creditLimit`/`creditDays` (`createCustomer()` collects neither) — lands in the safe "needs an advance" branch until an Admin sets credit terms from Parties afterwards.
  - **`recheckShortfallForSku()` (added 2026-09-22 for PDI, but lives here since the reservation data is Order FMS's own)**: wired into `src/lib/inventory/ledger.ts`'s `recordMovement()`/`recordMovementsBulk()` — after ANY FG "In" movement (from any source: a Production Line's final Action, a manual stock-in, a bulk import), best-effort (dynamic import, avoiding a circular import with the foundational `ledger.ts`) re-checks every order still short on that SKU and tops up `reservedQty`/`shortageQty` automatically, no manual "recheck" action needed. **Strict oldest-order-first (FIFO)** when two orders compete for the same newly-arrived stock — a deliberate, simple fairness rule (re-reads a fresh Free-stock snapshot before processing each order in `createdAt` order, so an older order's top-up is spent before a newer order even looks), not proportional splitting.
- **Module 18 — PDI / Pre-Dispatch Inspection (built 2026-09-22, `/pdi`)**: leg 3 of the Sales chain (Lead → Order → PDI → TMS → Dispatch — the user's original "4-leg" framing split into 5 once PDI/TMS/Dispatch turned out to need 3 separate modules, not one). A deliberately thin module — PDI owns **no** stock/money logic of its own. Intake mirrors Order FMS's own Lead-sourced queue exactly: `orders WHERE status='Ready_For_PDI' AND pdi_id=''`. Punching one in sets `dueAt = order.dispatchCommitDate − 1 day` (a sales deadline, plain calendar-day subtraction — no FMS working-hours calendar involved) and reads "waiting for stock" **live** off the order's own `order_items.shortageQty` every time, never stored on the PDI row itself. Inspection is Pass/Fail with an optional remark and an **optional report attachment** (per the user's explicit ask) — stored both on the activity (full history) and as a "latest" convenience copy on the inspection row itself. **A Fail loops the same row back to `Pending`** (logged as `Inspected_Fail`) rather than minting a new one — a fixable defect just gets re-inspected, by the user's own explicit choice over the alternative (an escalating Quality Hold). A Pass sets `Passed` and best-effort emits `emitFmsEvent("ORDER_PDI_PASSED", "ORDERS:<orderId>")`. No "PDI Setup" admin screen — judged not worth it for a genuine single-step module (Order/Purchase FMS's own per-step Doer isn't actually enforced either, only informational).
- **Module 19 — TMS / Transport Management (built 2026-09-22, `/tms`)**: leg 4. Retrofit into Order FMS first — `orders.transportArrangedBy` ("Self"/"Party", nullable for pre-existing orders) is now collected up front on every new order (`createDirectOrder()`/`createOrderFromQuotation()`), so TMS never has to guess it later; a pre-existing order with it still null surfaces in TMS's own intake as "needs a decision" rather than being silently excluded. **Transport Vendor Master** (`transport_vendors`) mirrors Purchase Vendor exactly (manual add + bulk import, warn-not-block on duplicates). One `planShipment()` function branches on `transportArrangedBy`: **Self** ("Freight Paid") requires a vendor/vehicle size/freight price and plans real cost; **Party** ("To Pay", the customer arranges their own pickup) forces those fields blank/zero — both produce the identical `tms_shipments` row shape, so "is this order's transport handled" is always one kind of query. **An order can need several shipments** (confirmed by the user — e.g. two trucks for one large order): `tms_shipment_items` tracks how much of each line rides on which vehicle, and **"fully shipped" is a live sum** of that quantity across every shipment for the order (regardless of Loading-Dock confirmation status — a *planned* truck already "spends" its allocation), never a stored flag. Loading Dock confirmation is the one action shared by both branches (fills in vehicle no/driver contact if not already known); Party's own extra action, Follow-up, is a log-only nudge with no field changes. Best-effort emits `emitFmsEvent("ORDER_FULLY_SHIPPED", ...)` once the last shipment is confirmed and the order reads as fully shipped.
- **Module 20 — Accounts / Receivables seed (built 2026-09-22, `/accounts`)**: a deliberately **narrow slice** of the still-much-bigger future Accounts module (Payables against a PO remains entirely separate, unscoped work) — one `invoices` row per order (E-way Bill, Invoice document, optional extra document, a final billed value). Candidates are Passed-PDI orders with no invoice yet — **independent of TMS's own state**, an order can be invoiced before, during, or after its shipments are planned. `finalValue` is **suggested** as the order's own value plus the sum of that order's TMS freight — **only when `transportArrangedBy === 'Self'`** (Party's freight is never this org's cost) — but stays a plain editable number, never silently locked. Reads `order_payments` (already built for Order FMS's own credit gate) to show what's actually been collected — no second payments table. An invoice is `Draft` until an invoice number *and* the invoice document are both attached, then `Issued` (immutable after). No activity-log table for this module — judged proportionate for a single-action flow, unlike every other leg's own timeline.
- **Module 21 — Dispatch (built 2026-09-22, `/dispatch`) — leg 5, the LAST leg. The Sales chain (Lead → Order → PDI → TMS → Dispatch) is now fully built end to end.** Intake is every `tms_shipments` row `At_Loading_Dock` with no `dispatch_id` yet (same per-shipment granularity TMS itself uses) — surfaces "waiting on Invoice" rather than hiding a candidate whose order hasn't been Issued yet, but `confirmDispatch()` still hard-refuses until it has. **Two real steps**: (1) **Confirm Dispatch** allocates a sequential Gate Pass number (same retry-on-collision allocator as `quotations.quotationNo`, backed by a real `(org_id, gate_pass_no)` unique constraint from day one) and — **this is the one genuinely load-bearing write in the entire Sales chain** — writes the real `stock_ledger` "Out" for exactly what the shipment carries. Nothing upstream (Order FMS's Stock_Check, TMS, PDI) ever touches the ledger, only reserves; this is where a reservation finally becomes a real, permanent stock movement. Not best-effort, same reasoning as an FMS Ledger Movement Action — every validation (shipment ready, Invoice Issued, real **on-hand** sufficiency — deliberately on-hand, not Free, since Free already excludes this order's own reservation and checking it here would double-subtract and wrongly refuse a valid shipment) runs before one atomic `db.batch()` writes the ledger rows, `order_items.consumedQty`, the dispatch row, the `tms_shipments` link, and the activity log together. Also assigns a tracker with a **per-instance TAT** (entered at confirm time, not a fixed org-wide Setup value) computed off that person's own working-hours calendar (`computeTatDeadline()`, same mechanism FMS steps use). (2) **Mark Dispatched** — the assignee or anyone holding `DISPATCH_FMS` closes it out, optional Proof-of-Dispatch attachment (transit-completion evidence — deliberately **not** customer-side Proof of Delivery, deferred by explicit user choice for this pass). An order reads as **fully dispatched** once every one of its shipments is `Dispatched` — live-computed, never stored, the terminal state of the whole chain.
  - **`orderItems.consumedQty` (added 2026-09-22) fixed a real double-count bug caught while designing this leg, before any code was written**: once Dispatch writes a real ledger "Out", that quantity is genuinely gone from on-hand — but nothing ever advances an Order FMS order's own `status` past `Ready_For_PDI`, so `orderReservedBySku()` would have kept counting the same quantity as "reserved" forever (once via the reduced on-hand, once via the still-full `reservedQty`), permanently understating Free stock with no way to ever clear it. Fixed by mirroring `plan_materials`' own `allocatedQty`/`consumedQty` split exactly — `orderReservedBySku()` now sums `reservedQty - consumedQty`. **Verified live**: reserving 20 of 50 on-hand correctly drops Free to 30; after the real dispatch, on-hand correctly drops to 30 but Free correctly *stays* at 30, not 10. Any future change to Order FMS's own reservation math must preserve this — it is the single most safety-critical invariant this whole Sales chain build produced.

**Live** at https://pro-erp-chi.vercel.app (Vercel, `Mking697/Pro-ERP` → auto-deploys on push to `main`). `GET /api/health` reports the live commit, DB configured status, and which other env vars are set (no longer reports Google service-account presence — irrelevant now).

**Production data note**: the Postgres cutover means the live database started **empty** the moment the migration deployed — any pre-migration Sheets-era organizations/users are not automatically carried over (there were none of real consequence at cutover time, only the owner's own test org, by explicit confirmation before the migration began). The very first thing after this deploy is a fresh `/signup`.

## Next up

Original spec for Inventory/BOM/PPC: **[docs/INVENTORY-PPC-PLAN.md](docs/INVENTORY-PPC-PLAN.md)** — still the source of truth for that subsystem's design decisions, unaffected by the storage migration.

**Purchase FMS is done** (see Module 14 above) — built as its own hardcoded flow rather than a generic FMS Template, so there is nothing left for the Admin to configure there.

**The first real Production Line is built and verified (2026-09-21)** — "Production Line 1" (`FTP-P13J3JW8`, 5 steps ending in a Ledger Movement Action), run end to end on a real plan (`PLAN-DM3VRFMT`, product `FG-SHOE-121`) and independently checked against live data: single stock writer confirmed (no double-count — see the working notes on this invariant), TAT calendar math correct including a lunch-break exclusion, pass-qty carried step to step. Two loose ends from that same run were left for the user to close themselves, not code bugs: one unit stuck in an overdue rework loop at Surge Testing, and the plan itself still needs "Complete Plan" clicked in the PPC UI (safe to do any time — `hasFmsLine()` will correctly skip the duplicate stock write). **More Lines, one per product family, still need building the same way** — repeat the pattern (Admin builds the template, plan picks it via `fmsTemplateId`, verify against a real run) for every other product.

**Inward's `Party_Name` → Vendor Master wiring is done** (2026-09-21, see Module 6 above) — type-ahead vendor picker + `vendor_id` column, free text still allowed for a not-yet-registered party.

**🎉 The Sales chain is fully built, all 5 legs — Lead → Order → PDI → TMS → Dispatch** (Modules 16-21 above), **plus a Receivables seed of Accounts.** The user's original framing was 4 legs (PDI+Dispatch as one); once actually scoped, PDI/TMS/Dispatch turned out to need 3 separate modules, by explicit user choice. Accounts was pulled forward from "later, unscoped" to "start now" mid-TMS-scoping, specifically as the Receivables documentation this same dispatch flow needed (E-way Bill/Invoice) — not the full ledger at the time. Every leg was scoped in its own conversation before any code, matching the discipline the very first one (Lead FMS) established.

**2026-09-22, second half of the day — everything that was "left" above is now done, in one large batch, each piece scoped by the user up front and built by a dedicated background agent, reviewed and independently verified by the session driver before merging:**
- **Proof of Delivery** (Module 21's own addition — see Module 21 above): `markDelivered()`, a third Dispatch status. Dispatch → PDI → TMS → the whole Sales chain now genuinely ends at "the customer has it," not "it left the building."
- **PO PDF generation** (Module 14's own addition — the "PO/document engine hand-over" item): `generatePoPdf()`/`previewPoPdf()`, same `@react-pdf/renderer` pipeline Quotations already use. Offered alongside the existing manual-upload option at PO Issue, not a replacement.
- **Module 22 — General Ledger + Payables** (`/accounts` → Payables/Ledger tabs, `src/lib/accounts/{ledger,payables}.ts`): real double-entry bookkeeping (`chart_of_accounts`/`journal_entries`/`journal_lines`, `postJournalEntry()` balances-or-throws) underneath the existing Receivables seed, auto-posted best-effort from four real events (invoice issued, payment recorded, bill issued, bill payment recorded). Payables (`bills`/`bill_payments`) mirrors Receivables exactly, against a Completed Purchase Order. Trial Balance/P&L/Balance Sheet are live-computed; the Balance Sheet adds one synthetic "Retained Earnings (Current)" equity line since there's no period-close mechanism yet (a documented judgment call, not a bug) — see `ledger.ts`'s own header comment before touching this.
- **Module 23 — Payroll v1** (`/payroll`, `src/lib/payroll/`): explicitly simple, explicitly NOT statutory-compliant (no PF/ESI/TDS — that's still a separate future conversation). "Attendance" means join-date proration only (no clock-in/clock-out exists anywhere in this codebase); an Inactive user zeroes for the whole run since `users` carries no deactivation timestamp (a real, documented limitation, not silently patched — a future session wanting exit-date accuracy needs to add that column first, which is a schema change to flag, not make in passing). Approved Leave never reduces pay, since the Leave quota below makes every leave within-quota by construction. Salary visibility is Role-gated (Admin console) rather than a module grant, on purpose — everyone can read their own payslips regardless of role.
- **Leave annual quota** (Module 15's own addition, `src/lib/leave/quotas.ts`): opt-in per org per leave type, hard-refuses a request that would exceed it, no carry-forward/accrual. Covered by `tests/leave-quota.test.ts`.
- **Billing/plan usage limits** (no payment gateway, by explicit choice): Free plan capped at 5 active users, Pro unlimited (`src/lib/platform/planLimits.ts`); Platform Admin changes an org's plan from `/platform`.
- **The ~4MB attachment cap is gone**: `FileUploadField` now uploads directly from the browser to Vercel Blob (`@vercel/blob/client`'s `upload()`, authorized by `/api/blob/upload`) — the file never passes through this app's own serverless function, so Vercel's body-size limit no longer applies. Replaced `/api/drive/upload` (deleted).
- **Mobile: PWA installability + real phone-width fixes**, not just an assessment — see its own bullet below, since there's still real follow-up work there.

**2026-09-24 — a full day of hardening plus real feature requests, each scoped by the user up front, built by a dedicated background agent, reviewed and independently verified before merging:**
- **Database indexing** (`src/db/schema/**`, 54 indexes across every tenant table, migration `0011`): the entire schema had zero secondary indexes — every `org_id` filter was a sequential scan. Composite indexes added for the specific hot paths a live-data audit found (`stock_ledger(org_id, sku)`, `fms_runs` ×4, `journal_entries(org_id, entry_date)`, `orders`/`order_items`/`quotations`/`leads`/`tms_shipments`, ...). Also fixed the FMS Flow Board pulling an org's entire run history and filtering in JS — now pushed into SQL (`listFmsRunsForTemplate()`).
- **Accessibility pass**: 13 tables had clickable rows with no keyboard path in — a mouse-only user (or anyone whose mouse just broke) could not open a single record. Fixed with a real icon-button-in-last-cell, mirroring the one working pattern that already existed (Orders' own candidates table). Also fixed a Lookup combobox's missing label association, three insufficient-contrast text colors, and one missing `DialogDescription`.
- **Security fix — Blob upload overwrite vector** (`src/app/api/blob/upload/route.ts`): the route only validated the client-declared MIME type and trusted the client-supplied `pathname` completely; since `@vercel/blob` defaults `allowOverwrite` to `true` when unspecified, and every tenant's attachments live in one shared Blob store, any signed-in user from any org could overwrite an existing blob at a known URL (e.g. an already-shared Invoice/PO PDF) with attacker content while the trusted URL stayed the same. Fixed: pathname restricted to the one shape the legitimate client ever sends (`attachments/<uuid>-<name>`), `allowOverwrite: false` explicit.
- **GST tracked as its own liability, not folded into Sales Revenue** (`GST_PAYABLE` account, `orders.gstPercent`/`gstAmount`, `invoices.gstAmount`): an issued Invoice's full GST-inclusive value used to book entirely to Sales Revenue, overstating profit by the tax portion collected on the tax department's behalf. Direct orders also gained a GST% field for the first time (previously carried none at all — Lead-sourced orders already had it via their own Quotation). Closed in the same pass: a check-then-insert race that could create duplicate Invoices/Bills for the same Order/PO (real unique constraints added), a missing PDI-Passed check inside `createInvoice()` itself (the exact same bug *class* as the original migration's `GET /api/fms/steps/[runId]` fix — a sibling endpoint had the check, this one didn't), and a Trial Balance/P&L/Balance Sheet date-filter bug (a bare `new Date("YYYY-MM-DD")` truncated to UTC midnight instead of end-of-day IST — the same bug class already fixed once in the recurring-task generator, recurred here, now fixed the same way).
- **Module 24 — Additional Payments & Petty Cash Book** (`/accounts` → Other Payments / Petty Cash tabs, `src/lib/accounts/{expenses,pettyCash}.ts`): a one-off Cash/Bank expense not tied to any Order/PO (Rent/Salary/Utilities/Misc/Transit-Loss categories, posted atomically, no Draft step), and a small imprest cash fund with its own GL account (`PETTY_CASH`, separate from `CASH_BANK`), Top Up/Expense actions, and a live-derived running balance that refuses to go negative.
- **Module 25 — Credit Notes & Debit Notes** (`/accounts` → Credit Notes / Debit Notes tabs, `src/lib/accounts/{creditNotes,debitNotes}.ts`): closes a real gap — goods lost in transit, or a customer return, previously had no way to reverse part of an already-Issued Invoice. A Credit Note credits a new `CUSTOMER_CREDIT_BALANCE` liability account (deliberately **not** Accounts Receivable directly — the original invoice may already be fully paid, in which case crediting AR would be wrong; see `ledger.ts`'s own comment on this account before touching it) whose value is then either **Applied** to a future order (inserted as a real `order_payments` row, `mode: "Credit_Note"` — Order FMS's own credit-gate math needed zero changes to treat it as a real payment) or **Refunded** in cash. Debit Notes are the exact Payables-side mirror (`VENDOR_CLAIM_RECEIVABLE` asset), built specifically for IQC failures: a Failure Log entry now has two independent actions — **"Accept Under Deviation"** (moves the failed quantity into stock anyway, as a documented quality concession, via a new `IQC_Deviation` ledger source distinct from a clean IQC pass) and **"Issue Debit Note"** (a claim against the vendor, later Applied against a future Bill via a real `bill_payments` row, `mode: "Debit_Note"`, or Received in cash).
- **Chart of Accounts "add account" + Manual Journal Entry** (`createAccount()`/`createManualJournalEntry()` in `ledger.ts`): closes the last documented gap the GL launched with — an Admin/Accounts holder can now add a non-system account on top of the 14 seeded ones, and post a manual correcting entry, reusing the existing `postJournalEntry()` balance-or-throw check rather than duplicating it. A manual entry is immutable once posted, same as every other entry in this Ledger.
- **Bell icon (Changelog) crash fixed**: `DropdownMenuLabel` renders Base UI's `Menu.GroupLabel` directly, which requires a `Menu.Group` ancestor — every other usage in this codebase (`settings-menu.tsx`) already wrapped it in one, this one (`changelog-menu.tsx`) didn't, and opening it threw an uncaught "Base UI error #31" with no error boundary to catch it, crashing the whole page for every single user who clicked it. One-line fix, but severity was real (every user, every click).
- **Select popup rendering off-screen inside a scrolled table, fixed** (`src/components/ui/select.tsx`): same root cause as the earlier nav-dropdown fix — `SelectContent` never exposed `collisionBoundary`/`collisionPadding`, unlike its `DropdownMenuContent` sibling. Found and fixed on the Platform org list's plan-change dropdown.
- **An interactive "System Map" artifact** exists and is kept current as features land — a clickable diagram of every module's own flow, every other module it connects to (with the exact condition, not just a label), where WhatsApp actually fires, and a live "what's pending / how to improve it" panel. Ask the user for the link if it's needed — it isn't tracked in this repo.
- **The GitHub PAT flagged in "Suggestions" item 6 below was pasted in plaintext chat AGAIN, twice, this same day** — see that item, now updated. This is the second session this has happened in; the recommendation hasn't changed, only the urgency has.

**2026-09-24, later the same day — IQC Deviation approval gate, Debit Note follow-up, and Credit/Debit Note safety hardening (user-requested, each item built and verified one at a time before moving to the next):**
- **"Accept Under Deviation" is now a real two-step approval, not a single unchecked click** — `src/lib/inward/deviation.ts` split into `requestUnderDeviation()` (any `IQC_CHECK` holder flags a failed quantity as wanted; does NOT move stock) and `approveUnderDeviation()`/`rejectUnderDeviation()` (gated to a new org-configured **Deviation Approver** — `INWARD_IQC_DEVIATION_APPROVER` setting, added to the same "Inward IQC" admin settings screen, mirroring Order Setup's `creditHoldApprover`/`approveCreditHold()` shape exactly — or an Admin). Stock only actually moves on Approve. `failure_log` gained 4 columns (`deviationRequestedAt/By`, `deviationApprovedAt/By`, migration `0016`) to hold this state; the Failure Log UI (`quality-records.tsx`) now shows Request → Pending Approval (+ Approve/Reject for the approver) → moved-to-inventory. Best-effort Task+WhatsApp notifies the approver on request, same fan-out shape as `notifyShortage()`.
- **Issuing a Debit Note now fires a best-effort 3-day follow-up Task + WhatsApp to every `ACCOUNTS_FMS` holder** (`notifyDebitNoteIssued()` in `debitNotes.ts`) — a nudge to keep chasing it until Applied to a Bill or Received in cash, since nothing else tracked that on its own.
- **Credit Note / Debit Note numbering retry-on-collision — done.** `credit_notes`/`debit_notes` now carry a real `unique(org_id, credit_note_no / debit_note_no)` constraint (migration `0017`); `createCreditNote()`/`createDebitNote()` retry the whole insert batch on a genuine 23505, mirroring `quotations.quotationNo`'s own pattern exactly. Verified live by forcing two concurrent creates against the same org — both succeeded with distinct numbers.
- **Credit Note / Debit Note balance-check TOCTOU race — done.** `applyCreditNoteToOrder()`/`refundCreditNote()`/`applyDebitNoteToBill()`/`receiveDebitNotePayment()` no longer read-then-check-then-write. Each now runs a single atomic `INSERT ... SELECT ... WHERE credit_notes.amount - SUM(existing usages) >= amount` statement (`insertCreditNoteUsageIfBalanceAllows()`/`insertDebitNoteUsageIfBalanceAllows()`) — Postgres itself evaluates the guard as part of the one statement, so the usage row is only ever inserted when real balance allows it, closing the race no read-then-write shape could. Deliberately NOT folded into the same `db.batch()` as the payment/journal-entry writes that follow — neon-http has no real transactions and a batch can't branch on an earlier statement's result (see `src/db/client.ts`), so the guarded insert is its own statement, executed first; only on success does the caller proceed to the payment+journal batch. Verified live by forcing two concurrent 60-against-100 applications on all four paths — exactly one succeeded each time, remaining balance landed exactly at 40, never overdrawn.
- **Real bug found and fixed while verifying the above — `isUniqueViolation()` was silently broken everywhere it existed** (7 copies: `creditNotes.ts`, `debitNotes.ts`, `ledger.ts`, `payables.ts`, `accounts.ts`, `dispatch.ts`, `quotations.ts`). It checked `error.code`/`error.constraint` directly, but this drizzle-orm version wraps the real driver error inside `DrizzleQueryError.cause` — the outer error's own properties are only `query`/`params`/`cause`. Every retry-on-collision safety net in this codebase (including `quotations.quotationNo`, already believed solid) was consequently never actually catching a real 23505 and retrying — it would throw the raw Postgres error straight through instead. Fixed in all 7 places to walk the `.cause` chain. **This means the "no retry loop, nothing would catch it" framing this doc used for CN/DN numbering before today was doubly true in a way nobody had noticed — the pattern itself, everywhere it was already used, wasn't working.**
- **Real bug found and fixed — `deleteOrganization()` didn't cascade into 6 newer tenant tables**: `credit_notes`, `credit_note_usages`, `debit_notes`, `debit_note_usages`, `expense_entries`, `petty_cash_entries` were all missing from its `db.batch()` list despite each having an `org_id` FK — exactly the "single most-recurring bug" pattern this doc's own working notes warn about. Deleting any org that had issued a Credit Note, Debit Note, Additional Payment, or Petty Cash entry would have thrown a raw FK violation instead of succeeding. Fixed in `src/lib/platform/registry.ts`.
- All four pieces above were built one at a time (a background agent for the first two, the session driver directly for the numbering-race verification/isUniqueViolation/deleteOrganization fixes after a background agent hit a session rate limit mid-task), each verified against the real Neon dev DB via a throwaway-org live-test script before moving to the next, per the user's explicit "one by one, resolve issues before continuing" instruction.
- **GitHub push access sorted out, on the user's own explicit terms** — see Suggestions item 6 below for the full story (a first attempt gave `Hikom8428` its own collaborator access, then was explicitly reversed the same day per the user's instruction that only the `Mking697` identity should ever be able to touch this repo; the actual fix was `gh auth setup-git` wiring `git` itself to authenticate via the active `gh` account, no second identity involved).

**2026-09-24/25 — the four items the System Map artifact's own "pending" panel had been carrying (user asked for all four "complete karke Artifact update karo"), each scoped, then built and verified one at a time (mostly by dedicated background agents; two were finished directly by the session driver after their agents hit a session rate limit mid-task) — this closes out that panel; see the System Map artifact itself for the after-state:**
- **Payroll exit-date proration — done.** `users.deactivatedAt` (nullable timestamp, migration `0018`) is now set/cleared by `updateUser()` on a real `Active → Inactive` / `Inactive → Active` transition. `computeDaysEmployed()` (`payroll.ts`) now prorates an Inactive user's pay up to (inclusive of) their own deactivation day instead of zeroing their whole month — the deactivation day counts as a full paid day, mirroring `createdAt`'s own inclusive join-day convention. An Inactive user with no `deactivatedAt` (pre-existing data from before this column existed) still zeroes for the month — the one honest, documented fallback this doesn't solve. Still explicitly NOT statutory (no PF/ESI/TDS) — that remains separate, unscoped, future work.
- **Multi-invoice-per-order splitting — done.** `invoices.invoices_org_id_order_id_unique` dropped (replaced by a plain index, migration `0019`) — an order can now carry several invoices instead of exactly one. `createInvoice()`/`updateInvoice()` gate against a live-computed "remaining invoiceable value" (`totalInvoiceable − sum(finalValue across every existing Draft+Issued invoice for the order)`) instead of "refuse if any invoice exists"; `listInvoiceCandidates()`/`getInvoiceSuggestion()` both now understand a partially-invoiced order stays a candidate for its next split invoice. GST is prorated to each invoice's own share of the order total, capped so the running sum across every invoice for an order never exceeds `order.gstAmount` (verified live: a 1000/gst180 order split 600+400 produced GST exactly 108+72=180, no rounding drift). `isInvoiceIssued()` (Dispatch's own gate) and `createCreditNote()` (keyed to one specific `invoiceId`) both needed zero changes — confirmed correct as-is. **No unique-constraint backstop remains against a genuine concurrent-request race on the remaining-value check** — the same low-risk, human-triggered judgment call this doc already documents for Credit/Debit Note numbering, flagged the same way in `accounts.ts`'s own comments.
- **The PO/document engine gap — done, scoped down to what was actually missing.** The user handed over `Mking697/Next-GEN-CRM` (the same reference already used for Quotations) as the reference to learn from again; it turned out to have no Purchase Order concept of its own (it's a Quotation-only CRM) — comparing Pro-ERP's own `quotationPdf.tsx` against its own thinner `poPdf.tsx` directly showed the real, concrete gap: no GST/Sub-Total/Grand-Total breakdown, no Note, no Terms & Conditions section on a PO, unlike a Quotation. Closed: `purchase_orders` gained `gstPercent`/`termsAndConditions`/`note` (snapshotted at Issue time, migration `0020`), Purchase Setup gained matching org-wide defaults (`PURCHASE_GST_PERCENT_DEFAULT`/`PURCHASE_DEFAULT_TERMS`/`PURCHASE_DEFAULT_NOTE` — deliberately separate settings keys from Quotation's own, so editing one never silently changes the other), and `poPdf.tsx` now renders the same Sub-Total/GST%/Payable-Amount breakdown and Note/Terms sections `quotationPdf.tsx` already does. The PO Issue screen shows/lets the purchaser override all three before Issue. Deliberately did NOT add a bank-details section (a PO is money going out, not in — Quotation's own bank block exists so a customer can pay the org, which has no PO-side equivalent) and did NOT add per-line GST/HSN codes (not tracked anywhere in this codebase, a much bigger unscoped change).
- **Receivables Aging + GST Return report — done, both narrower than "the full future Accounts module" but real.** Aging (`getReceivablesAging()`) deliberately ages at the ORDER level, not per-invoice — `order_payments` is keyed by orderId, not by any specific invoice, and multi-invoice split means there's no clean way to attribute a payment to one invoice among several, so an order's own total outstanding (every Issued invoice's `finalValue`, minus every `order_payments` row for it) is aged from its earliest Issued invoice's date, bucketed 0-30/31-60/61-90/90+. GST Return (`getGstReturnSummary()`) is a GSTR-1/GSTR-3B-**shaped** report/export for the org's own accountant to manually file — explicitly NOT e-filing (no GSP/ASP API, no digital signature, nothing submitted anywhere), and explicitly OUTPUT GST only (Input Tax Credit from the org's own Bills/Payables isn't tracked anywhere in this codebase — `bills` carries a flat `amount`, no GST split — so this is a liability figure, not a net-payable one; the UI says so). A CSV export (`buildCsv()`/`csvResponseHeaders()`, `src/lib/csv.ts` — the same helper the bulk-import templates already use) gives the line-item table in the shape an accountant can actually import elsewhere. Both live on two new `/accounts` tabs (Aging, GST Report). Verified live: a 1000/gst180 order split into two invoices (600/400) correctly ages as one 500-outstanding row after a 500 partial payment, while a separately fully-paid order correctly drops out of the report entirely; the GST report's date-range filter correctly includes/excludes invoices by `issuedAt`.

**2026-09-25, later the same day — the two items still left in the System Map's own pending panel, closed one at a time, each scoped up front with the user before building:**
- **Input Tax Credit tracking (Payables-side GST) — done.** `bills` gained `gstPercent`/`gstAmount` (migration `0021`) — deliberately the OPPOSITE direction from `invoices`: a Bill's own `amount` is the GST-**inclusive** total the vendor actually invoiced (mirroring `invoices.finalValue`'s own contract), and `gstAmount` is **extracted** from it (`amount − amount / (1 + gstPercent/100)`), not added on top — a vendor's real invoice already includes their GST, there's no separate exclusive base to start from the way an Order/Quotation has. Defaults from the PO's own `gstPercent` (added the same session in the PO document engine work), overridable per Bill. `issueBill()`'s GL posting now splits `Dr Purchases Expense (amount − gstAmount) / Dr GST Input Credit (gstAmount) / Cr Accounts Payable (amount)` instead of folding the full GST-inclusive amount into Purchases/COGS — a new `GST_INPUT_CREDIT` (Asset, code `1160`) system account, the exact Payables-side mirror of `GST_PAYABLE`. `getGstReturnSummary()` (`accounts.ts`) now reads both sides: `billLines`/`totalInputGst` alongside the existing `lines`/`totalGst`, and a real `netGstPayable = totalGst − totalInputGst` (can go negative — more Input Credit than Output GST collected that period — a legitimate real-world state, not a bug). `listBillCandidates()`'s own suggested `poValue` is now GST-inclusive too, so it matches what a Bill's `amount` actually represents. Verified live: a Bill for ₹1180 at 18% GST-inclusive extracted to exactly taxable ₹1000 / GST ₹180, with the GST Report correctly showing it as Input GST and a negative net-payable (no Output GST that period). GST return **e-filing** (a real GSP/ASP integration) stays explicitly out of scope — confirmed directly with the user, since it needs real third-party account credentials this session has no access to.
- **The mobile audit's one remaining gap (Purchase/TMS/Dispatch candidates, Platform org list, all previously only checked near-empty) — closed.** Seeded real data for all four (an Approved Indent, two PDI-Passed orders taken through TMS/Dispatch, three extra throwaway orgs) and checked each at 390×844, including their own key dialogs (PO Issue's full candidate table, TMS's Plan Shipment dialog, Dispatch's Confirm Dispatch dialog, and Platform's plan-change `Select` specifically re-tested on a non-first row, the exact control this doc's own working notes flag as previously buggy). **Zero real bugs found — no code changes needed.** This closes the full mobile audit completely; there is no longer a known-thin-coverage gap anywhere in the app.

**2026-09-25, later still — the two remaining "deliberate choice" cards in the System Map's own pending panel, both closed, user-requested:**
- **Tap target size — done.** `src/components/ui/button.tsx`'s icon-size variants (`icon-xs`/`icon-sm`/`icon`/`icon-lg`, 24/28/32/36px) each gained an invisible `::before` hit-area, active only under `pointer-coarse:` (Tailwind's `@media (pointer: coarse)` variant — a real mouse never matches this, so desktop's existing dense table/toolbar layouts are pixel-for-pixel unchanged). The inset per variant is sized so every one reaches ~44px total on a touch device — the mobile tap-target guideline this card itself named. The inset amounts were chosen against `TableCell`'s own `p-2` (8px) padding specifically: `icon-sm` (28px, the size every row-action button in this codebase actually uses — confirmed by grepping every `size="icon-sm"` call site) gets exactly an 8px inset per side, landing at 44px while never spilling past the cell's own existing padding into a neighboring cell's content. This was deliberately scoped to a `pointer-coarse:` media condition rather than a blanket size increase, specifically to avoid the regression the pending card itself flagged as the reason this was left alone before: a flat 44px icon button would have visibly bloated every dense desktop table this session's own accessibility pass just finished fitting icon-buttons into.
- **Custom pickers (Vendor/Item/Lookup) keyboard-ARIA pattern — done.** New shared hook `useComboboxNav()` (`src/components/ui/combobox.tsx`) gives Arrow Up/Down to move a highlighted result, Enter to pick it, and Escape to close the list without closing whatever Dialog it's inside (`stopPropagation` — a bare Escape bubbling up would otherwise also close the parent Dialog). Wired into all three pickers this card named by their own file names: `src/components/vendor-picker.tsx`, `src/components/item-picker.tsx`, and FMS's `LookupCombobox` in `src/app/fms/complete-step-dialog.tsx` — each gained `role="combobox"`/`aria-expanded`/`aria-controls`/`aria-activedescendant`/`aria-autocomplete="list"` on its `<Input>` and `role="listbox"`/`role="option"`/`aria-selected` on its result list, plus a highlighted-row style so the keyboard state is visible, not just announced. The highlighted index is clamped to the current result count at **read time** (`Math.min(rawIndex, itemCount - 1)`), not reset via a ref-comparison-during-render or a `useEffect` — this project's lint config (`react-hooks/refs`, `react-hooks/set-state-in-effect`) disallows both of the usual patterns for "reset this state when some other value changes," and a pure clamp turned out simpler anyway: it can never point past the end of a list a keystroke just narrowed, with no extra state-sync code needed. `AutocompleteInput` (UOM/Size free-text fields) and `CustomerPicker` (Order/Lead's Existing-Customer tab) use a visibly different pattern (an always-open inline list, not a collapsible popover) and were deliberately left as-is — this card named three specific pickers by their popover-combobox shape, not every type-to-search input in the app.

**What's still actually open, now:**
- The full future Accounts module beyond this GL (multi-invoice-per-order splitting, an Aging report, a GST-return-shaped export, and Input Tax Credit tracking are all done now — see the dated bullets above). What's genuinely still unbuilt: real GST return *filing* (GSP/ASP integration — explicitly out of scope, confirmed directly with the user, the export above is deliberately manual-file-by-your-CA instead) and AR aging's own natural next step of per-customer credit-limit-vs-aging cross-checks. Today's GL is real double-entry with Receivables (multi-invoice-capable), Payables (Input Tax Credit-capable), Credit/Debit Notes, Petty Cash, manual entries, Aging, and a GST export with a real net-payable figure — a genuinely solid foundation, still a seed relative to a full accounting system.
- ~~Credit Note / Debit Note numbering has no retry-on-collision~~ / ~~Credit Note / Debit Note balance checks have a small TOCTOU race~~ — **both done 2026-09-24**, see the dated bullet above.
- ~~The PO/document engine hand-over proper~~ — **done 2026-09-25**, scoped down from "the user's own crm.hicon.co.in tool as a bigger integration" to the concrete GST/Terms/Note gap that comparison against the reference actually surfaced — see the dated bullet above.
- Every "Setup" screen this chain has (Purchase Setup, Order Setup) is Doer/TAT-as-organizational-config; PDI and Dispatch both deliberately skipped one — PDI because Doer/TAT isn't actually enforced anywhere in this codebase's precedent, Dispatch because its own TAT is explicitly per-instance (entered at Confirm-Dispatch time), not org-wide config.
- ~~Payroll has no `users.deactivatedAt` column for exit-date accuracy~~ — **done 2026-09-25**, see the dated bullet above. Payroll still has no PF/ESI/TDS (explicitly deferred, a separate future conversation, not started).

**Mobile — PWA (2026-09-22) + the full screen-by-screen audit (2026-09-25) are both done now.** `public/manifest.json`/`sw.js`/`offline.html`/icons make the app installable (verified via real headless-Chrome CDP sessions against `npm run build && npm run start` — manifest 200s, SW reaches `activated`); the SW is deliberately a conservative app-shell cache, network-first for everything else, so it can never reintroduce the stale-tenant-data problem this codebase spent real effort removing when Sheets-era caching was dropped. Two real bugs were found and fixed along the way: `src/proxy.ts`'s matcher was silently 307-redirecting the manifest/SW/icons to `/login` for anyone not already signed in (broke installability for exactly the first-time-visitor scenario that matters most), and the SW's own registration listener frequently never fired due to a `load`-event race. `/tasks`' header row and the nav dropdown's phone-width clipping (see Module 12's nav bullet) were the two real bugs the first pass found and fixed.

**The full audit (2026-09-25) took three attempts to actually finish** — worth remembering as a pattern, not just a result. Attempts 1 and 2 both failed for genuine environment reasons, not app bugs: attempt 1 hit repeated 8-10 minute `next dev`/Turbopack "filesystem cache database compaction" stalls (every request hangs, even a plain GET) that ended in a fatal internal Turbopack panic, worsened by 4 other agents' concurrent builds competing for the same machine at the time; attempt 2 didn't even get to start, killed by a session-wide API rate limit. Attempt 3 succeeded by doing what attempts 1 and 2 both learned the hard way: skip `next dev` entirely, drive the audit against `npm run build && npm run start` instead, and run it only once the machine was quiet (no other concurrent agent builds). **Result: every page in the app, checked at 390×844 — zero real overflow/clipping bugs found**, across both passes combined (`/dashboard`, `/tasks`, `/leave`, `/bom`, `/ppc`, `/pdi` from pass one; every other route — `/accounts` all 12 tabs including the new Aging/GST Report tabs, `/admin/settings`, `/admin/users`, `/dispatch`, `/fms` + template builder + step-complete dialog, `/inventory` + `/inventory/fg` + `/inventory/[sku]`, `/inward`, `/leads` + the quotation line-item builder — the single structurally highest-risk page in the app, `/orders`, `/parties`, `/payroll`, `/performance`, `/platform`, `/purchase`, `/reports` + sample report pages, `/tms`, `/login`, `/signup` — from pass three, each with at least one representative dialog/modal opened too, seeded with real data specifically so tables wouldn't render empty). No code changes were needed in pass three — the app was already clean everywhere it was actually tested. **The one narrower gap this pass left — `/purchase`/`/tms`/`/dispatch`'s own candidates views and `/platform`'s org list, only checked near-empty — was closed the same day**: seeded an Approved Indent, two PDI-Passed orders, and three extra throwaway orgs, checked all four (plus their own key dialogs) at 390×844, zero bugs found. The mobile audit is now fully complete with no known-thin coverage anywhere. The literal "Add to Home Screen" prompt firing in a real session over time, and whether a wrapped/native app is ever wanted instead of/alongside the PWA, remain outstanding — both are product decisions, not technical ones.

**Reports & sharing** (`/reports`, `/reports/<id>`, driven by `src/lib/reports.ts` — a single registry so the index, each page, the share dialog, and the public share page never drift): a report the reader has no grant for returns 404, not 403. A share link (`/share/<token>`) resolves its tenant from the token alone, never the session cookie; the creator's grants are copied onto the link at creation time, not read live, so a later grant change can't silently widen an old link; revoking deletes the row outright. Unaffected in behavior by the migration, only in storage — `report_shares` is now a real table (was a `Report_Shares` tab on the platform spreadsheet), and its lookup is no longer cached (a revoked link now stops resolving immediately instead of within 30s on some serverless instances).

**Users and organizations can be deleted.** Deleting a user removes the row and frees the email in the platform index, but leaves their tasks and records alone (two refusals: deleting yourself, deleting the last active Admin). **Deleting an organization is now a real, permanent, full data wipe** — `deleteOrganization()` cascades into all 21 tenant-owned tables via one atomic `db.batch()`. This is a genuine behavior change from the Sheets era (where an org's own spreadsheets were simply never touched) — the confirmation UI was updated to say so accurately; don't let a future edit quietly restore the old "your data is safe" wording.

Not done yet:
- `organizations.systemSheetId` and every other Sheets-era vestige are gone from the schema — if a doc, memory, or comment anywhere still references a "System spreadsheet", `PLATFORM_SHEET_ID`, or "paste a Sheet URL", it predates the migration and is wrong; fix it in place rather than propagating it further.
- **Fixed 2026-09-22 — a buddy's own TAT calendar is now recomputed on reassignment.** `recomputeRunTat()` (`src/lib/fms/engine.ts`, exported alongside `queueOpenTatStart`/`resolveTatValue` which it reuses) recomputes a Pending run's `TAT_Start`/`TAT_Deadline` fresh from "now" against the new assignee's own working calendar (shift, lunch/tea, weekly-off/holidays, their own open-TAT queue) — called from both `activateLeave()` and `revertLeave()` (`src/lib/leave/reassignment.ts`, via a dynamic `import()` to break the same leave<->fms circular-import shape `plans.ts`<->`fms/engine.ts` already avoids). A no-op once a run has left Pending. Covered by `tests/leave-reassignment.test.ts`'s second test.
- ~~No billing~~ / ~~Leave has no balance/quota tracking~~ / ~~Attachment uploads capped at ~4MB~~ / ~~Payroll System~~ — **all done 2026-09-22**, see "Next up" above for the full rundown of that batch. Payroll remains explicitly non-statutory (no PF/ESI/TDS) — that's still a separate future conversation, not started.
- A `tsx`-run live-test script that renders a real `@react-pdf/renderer` document (Quotation, PO, or Payslip PDF) will throw `ERR_PACKAGE_PATH_NOT_EXPORTED` on `@react-pdf/hyphenate` — a CJS-transpilation quirk specific to `tsx`, confirmed independent across two separate sessions building PO PDF and Payslip PDF the same week. The actual Next.js production build resolves this fine (confirmed via `npm run build` both times); it's a gap in the `scripts/*.ts` live-test convention itself for any PDF-generating code, not a real bug. Wrap the PDF-rendering step in its own try/catch when live-testing a feature that generates one, and don't treat that step's failure under `tsx` as evidence the feature is broken — verify the PDF pipeline separately (e.g. a standalone `.mts` probe, as one of the two sessions did) if it actually needs proving.

### Scoped and approved 2026-09-25 — Paid plan tiers + a per-tenant AI Chatbot (business-side planning, nothing built yet)

The user (Essor Automations, the company behind Pro ERP) ran a multi-turn planning conversation covering go-to-market pricing, real per-tenant infrastructure-cost measurement, and a new AI Chatbot feature — competitor pricing was researched live (Zoho Books, TallyPrime, Vyapar, Marg ERP, ERPNext/Frappe Cloud, Odoo, Katana MRP) and real Neon/Vercel/AWS RDS/DigitalOcean/Hetzner pricing was checked, before anything below was approved. **Everything in this section is scoped and approved, not implemented** — it is the plan for an upcoming session, written here so that session picks up with full context rather than re-deriving it. Promotional materials for Pro ERP (flyers/brochure/pitch deck/social posts, in Essor Automations' own black-and-red brand) exist as private Claude Artifacts from this same planning arc — ask the user for the links if they're needed; they aren't tracked in this repo.

- **Plan tiers, replacing today's flat Free(5 users)/Pro(unlimited) split** (`src/lib/platform/planLimits.ts`): a **14-day free trial** (full feature set, explicitly chosen over a permanent free tier — the user wants every real account to convert to paid, not sit on a ₹0 plan indefinitely), then **Growth** (₹3,999/month launch price, rising to ₹5,999/month after an introductory period — mirroring Odoo's own promo-then-renewal pattern — up to 20 users then ₹249/extra user/month, 1 company, every module), **Scale** (₹14,999/month, unlimited users, up to 3 companies then ₹4,999/extra company/month), and **Enterprise** (custom pricing, unlimited everything, priced for once the app is off Vercel/Neon onto dedicated infra). These are launch hypotheses to validate against real prospects, not final numbers.
- **Module-gating by tier, on top of today's per-user grants** — a module must be both (a) allowed by the org's plan tier and (b) granted to the specific user via today's existing `Module_Access`/`requireModule()` mechanism. This is a new check sitting alongside the existing one, not a replacement for it — today's grant system controls *who inside an org* can use a module the org's plan already allows.
- **Per-tenant usage measurement, because the shared-schema architecture has no native per-tenant bill.** Neon bills the whole endpoint's total compute-hours and total storage-GB — it has no concept of "tenant." A new `tenant_usage_metrics` table plus a small job added to the existing daily cron (`runDailyJobs()`) should compute, per org: a **storage share** (row-counts per org per tenant-scoped table, `count(*) group by org_id`, multiplied by each table's own periodically-measured avg-bytes-per-row) and a **compute/request share** (request volume per org, since `getTenantOrgId()` already resolves on every request — Neon exposes no finer-grained per-query compute metric than that to attribute against). Surfaced read-only on `/platform` for the Platform Admin. This is deliberately the same measurement approach whether the database stays on Neon or the app later moves to a self-managed Postgres on a VPS (Hetzner-style) or a managed service (AWS RDS/DigitalOcean) — none of those bill per-tenant either, so the same storage-share/request-share attribution is what will size and cost-allocate the post-migration infrastructure too, which is exactly why it's worth building against Neon now rather than waiting for the migration.
- **AI Chatbot, per-tenant Gemini API key** (new Admin → Settings section, mirroring the existing ChatXFlow WhatsApp token/phone/base-URL pattern — a per-org key in the `settings` table): an Admin pastes their own Gemini API key (free-tier or a paid/billing-enabled key — both are accepted identically, quota/rate-limits are between the tenant and Google, not something this app distinguishes), with a new in-app Guidebook chapter walking through getting one from Google AI Studio. Gated behind its own new module grant (not automatic for every signed-in user) — an Admin decides who gets chatbot access, the same as any other module.
  - **Three-layer access boundary, all inherited from existing checks rather than a new access system:** (1) **module-scoped** — the chatbot's available tool-functions for a session are filtered to only the modules the asking user currently holds a grant for (an Inward/IQC/PPC-only user's chatbot literally has no tool that can reach Purchase or Accounts data — Gemini is never even offered those tools, not merely instructed not to use them); (2) **always-available own-data** — regardless of module grants, a user can always ask about their own pending Tasks, their own MIS score (with a real per-task breakdown of *why* — which tasks were on-time/late/missed — reusing the existing scoring computation, not a new one), and their own assigned steps in any Flow/FMS instance, exactly mirroring how Tasks and a user's own dashboard need no module grant in the app today; (3) **system-only, no general knowledge** — the assistant declines anything not about Pro ERP's own data (e.g. "who is India's Prime Minister" gets "ask Google," not an answered) even though Gemini itself could answer it; enforced by both a system-prompt instruction *and* a stricter app-layer check that suppresses any response Gemini generated without calling at least one of the provided tool-functions, since a prompt-only restriction alone is not reliably jailbreak-proof.
  - **Chatbot is architecturally just another authenticated caller of the app's own existing access-checked functions** — it is never given raw database/SQL access. Every "tool" it can call is a real function that already runs through `requireModule()` and `getTenant()`/tenant-scoping exactly like a normal page or API route, so cross-user and cross-tenant leakage is prevented by the same mechanisms already trusted everywhere else in this codebase, not by a new, separately-fallible guard.
  - **Eight explicitly-approved improvements for this same v1** (not a later phase): the existing Guidebook content (`src/lib/guide.ts`, already filtered per-viewer by `guideFor()`) is a second safe answer-source alongside live data, for "how do I..." questions; a two-way WhatsApp channel (reusing the existing `sendWhatsAppMessage()`/ChatXFlow plumbing) so a user can ask the chatbot by sending it a WhatsApp message, not only from inside the app; access-scoped suggested-questions shown on first open (generated from the asking user's own live grants); multi-turn follow-up-question context within one chat session; a hard rule that the chatbot must say "I couldn't find that" rather than ever inventing a number when a tool call fails or returns nothing; a per-org audit log of every chatbot query (which tools were called, by whom) for the Platform Admin, the same spirit as `error_logs`; a Gemini-quota warning (plus the app's own sensible daily-message cap per org, reusing the `rateLimit.ts` pattern) so one user can't silently exhaust an org's whole free-tier quota; and a clear "AI Assistant isn't connected yet — ask your Admin to add a Gemini key in Settings" state when no key is configured, instead of a broken-looking feature.
  - **Deliberately excluded from v1, by explicit user decision**: the chatbot taking real actions (marking a task complete, etc.) rather than only reporting — read-only first, until it's proven trustworthy; a write-capable version, if it happens at all, needs its own explicit confirm-before-executing UX and is a separate, later scoping conversation.

## Suggestions for making the whole system better

The user asked for a genuine recommendation, kept current, on where the system as a whole could improve — not just a running list of finished features. In priority order, as of this update:

1. ~~Automated regression tests, not just disposable live-test scripts~~ — **done 2026-09-21**. `tests/*.test.ts` (Vitest) covers exactly the four areas this bullet named — `deleteOrganization()`'s cascade, Leave reassignment/revert, the recurring generator's Sunday/holiday math, FMS's working-hours calendar — each against the real Neon DB via its own throwaway org, and `.github/workflows/ci.yml` runs the full suite (plus tsc/lint/i18n:check/build) on every push to `main` and every PR. Needs the `DATABASE_URL` repo secret set in GitHub (done) to actually run.
2. ~~Move off `drizzle-kit push` onto real migration files~~ — **done 2026-09-21** (see "Migration tooling" above). `drizzle-kit generate` + `drizzle-kit migrate` is now the workflow, with a real baseline migration and commit history going forward.
3. ~~Error monitoring/observability in production~~ — **done 2026-09-22**, self-hosted rather than a third-party service (the user's explicit choice over wiring up Sentry — no new external account/DSN). `src/instrumentation.ts`'s `onRequestError` hook (Next's own global catch-all for anything an uncaught Route Handler/Server Component/Server Action throws) writes into a new `error_logs` table via `src/lib/errorLog.ts`'s `logError()` — always best-effort, never throws itself. The org is recovered by parsing the session cookie straight out of the hook's raw request headers (there's no `runWithTenant()` context this deep in Next's own error path). Two explicit call sites cover failures that were already caught and silently swallowed before this: the daily cron's per-organization failures (`forEachActiveOrganization`'s own result was never actually watched by anything) and a genuine WhatsApp send failure (`sendWhatsAppMessage()` still never throws by contract, but now logs on `!ok` from inside itself rather than leaving every caller to notice on their own). Readable at `/platform` → **Server Errors** (Platform Admin only, read-only, click a row for the full stack). `error_logs.orgId` is deliberately a plain string, not a foreign key — this is diagnostic/audit data meant to outlive an org's own deletion, so it is intentionally left OUT of `deleteOrganization()`'s cascade (the one exception to that rule's usual "every new tenant-scoped table" scope, because this was never a tenant-owned table). Covered by `tests/error-log.test.ts`.
4. ~~Assess mobile properly, as its own scoped piece of work~~ — **done, fully, 2026-09-25** (see "Next up" → Mobile above): PWA installability (2026-09-22) plus the full screen-by-screen audit at 390×844 (2026-09-25) — every page checked, zero real bugs found. This bullet is closed; the one remaining item (candidates/org-list views only checked near-empty) is narrow enough to be its own small follow-up if ever needed, not a reason to reopen this.
5. ~~Rate limiting / abuse protection on public-ish endpoints~~ — **done 2026-09-22**. `src/lib/rateLimit.ts`'s `checkRateLimit()` is a plain Postgres-backed fixed-window counter (`rate_limit_hits`, a new platform-wide table alongside `usersIndex`/`reportShares` — no in-memory state, since Vercel's serverless functions share no memory for that to live in). Wired into `/api/auth/login` (10/5min, keyed by IP+email together), `/api/signup` (5/hour, keyed by IP), and `/share/[token]` (60/5min, keyed by IP). Deliberately simple — a fixed window, not sliding/token-bucket, and no defense against a distributed attacker rotating IPs; good enough to stop a naive brute-force/scrape loop, which is all this was scoped to fix. Covered by `tests/rate-limit.test.ts`.
6. ~~Revoke/rotate the GitHub PAT that was pasted directly in chat~~ — **the root cause fixed 2026-09-24, on the user's own explicit terms.** First attempt (giving `Hikom8428` its own collaborator access on `Mking697/Pro-ERP`) was tried and then explicitly reversed the same day, per the user's own instruction: **`Hikom8428` must have zero access to, and zero connection with, this repo — everything stays on the `Mking697` identity alone.** `Hikom8428` was removed as a collaborator (`gh api -X DELETE repos/Mking697/Pro-ERP/collaborators/Hikom8428`, confirmed via a fresh `gh api repos/Mking697/Pro-ERP/collaborators` — only `Mking697` listed). The actual fix instead: `gh auth setup-git` was run, which wires `git`'s own credential helper for `github.com`/`gist.github.com` to `gh auth git-credential` (confirmed in `~/.gitconfig`) — so any `git push`/`pull`/`fetch` on this machine now transparently authenticates as whichever `gh` account is active (`Mking697`, confirmed via `gh auth status`), with no PAT ever typed or pasted again. This is what actually closes the loop the PAT was standing in for. **The old exposed PAT itself is still not revoked** — no GitHub API exists to enumerate or revoke a personal access token (classic or fine-grained) from the CLI; it's web-UI-only, so this one piece is still on the human who generated it: go to https://github.com/settings/tokens (on the account it was created under) and revoke it, now that nothing needs it.
7. ~~A lightweight in-app changelog/"what's new" surface~~ — **done 2026-09-21**. Bell icon in the header (`src/components/changelog-menu.tsx`), data in `src/lib/changelog.ts` — edit that file by hand each time a user-facing feature ships.

## Working notes for future sessions

- **Every Postgres query touching a tenant-scoped table must filter by `org_id`, sourced from `getTenantOrgId()`/`getTenant()` (`src/lib/tenant.ts`) — never from client input.** For a table with a plain `id` primary key, use `src/db/repo.ts`'s generic functions, which enforce this by construction. For a composite-PK table (`fms_templates`, `bom`, `plan_materials`, `holiday_list`, `fms_weekoff_overrides`), the file doing the bespoke query is fully responsible for adding `eq(table.orgId, orgId)` itself — this is exactly the class of file a security review should re-check first when anything in it changes.
- **`getTenant()` throws when it cannot resolve an org — it must never guess.** An explicit `runWithTenant()` context wins (used by cron), otherwise the session cookie decides. This rule predates and survived the migration unchanged.
- **Schema changes go through `src/db/schema/**` + `npx drizzle-kit generate` then `npx drizzle-kit migrate`, and are reviewed/applied directly by whoever is driving the session rather than by a subagent in passing.** (Before 2026-09-21 this was `drizzle-kit push`; see the "Migration tooling" note above for why and how the switch happened.) The convention this migration established: a subagent doing domain-file work that finds a real schema gap or bug (a missing column, a wrong type) stops and reports it rather than editing the schema itself; the session driver reviews and applies the fix, then migrates it live before the domain file gets adjusted to use it. This happened for real three times before the switch (a missing `plan_materials.uom` column, `fms_templates.next_step_map`'s string-vs-jsonb mismatch, and the new `notify_on_complete` column) — none of them caused a production incident specifically because a subagent flagged rather than silently patched around them.
- **Two tables deliberately break the "every table with `org_id` needs a `deleteOrganization()` cascade entry" rule below, on purpose: `rate_limit_hits` and `error_logs`.** Neither is tenant-owned business data — a rate-limit counter is keyed by IP/email, not an org, and an error log is diagnostic/audit history meant to survive the org it happened in being deleted later. Neither has an `org_id` foreign-key constraint at all (an `error_logs.orgId` is a plain string), specifically so deleting an org never throws over either of them. Don't "fix" this by adding them to the cascade — that would be the actual regression.
- **`deleteOrganization()` (`src/lib/platform/registry.ts`) must cascade into every *tenant-owned* table that has an `org_id` FK to `organizations`.** Adding a new tenant-scoped table to the schema and forgetting to add its delete to this function's `db.batch()` means deleting any org that has touched that table throws a raw FK-violation instead of succeeding — this was the single most-recurring bug across the whole migration (hit and re-hit at three different points as new tables landed, then a fourth time on 2026-09-24 when `credit_notes`/`credit_note_usages`/`debit_notes`/`debit_note_usages`/`expense_entries`/`petty_cash_entries` turned out to have shipped without ever being added here — fixed the same day). When you add a table, grep this function and add it there in the same change.
- **Any `isUniqueViolation(error, constraintName)` helper in this codebase must walk `error.cause`, not read `error.code`/`error.constraint` directly.** This drizzle-orm version wraps the real Postgres driver error (which carries `code`/`constraint`) inside a `DrizzleQueryError` whose own properties are only `query`/`params`/`cause` — checking the outer error's fields directly never matches, so the retry-on-collision loop built on top of it silently never retries and just throws the raw 23505 through instead. This was true of all 7 copies of this helper in the codebase (`quotations.ts`, `dispatch.ts`, `ledger.ts`, `payables.ts`, `accounts.ts`, and the two new Credit/Debit Note files) until fixed 2026-09-24 — including `quotations.quotationNo`'s own retry loop, previously assumed solid and never actually re-verified live. If a new file ever needs this helper, copy the fixed (cause-walking) version, not an older copy of this function found by grepping the codebase.
- **`npx drizzle-kit push` is retired (2026-09-21) — don't use it, use `generate`+`migrate` instead (see "Migration tooling" above).** It's also the command that produced two real incidents on the same day, both worth remembering even though the tool itself is no longer in the workflow: (1) it cannot run non-interactively in this environment — it always stopped to ask "You're about to add `settings_org_id_key_unique` unique constraint... Do you want to truncate settings table?" even after that exact constraint already existed in the live DB (a real drizzle-kit introspection quirk on this composite unique constraint, confirmed via `pg_constraint`), and `--force` would have auto-approved whatever that prompt needed, up to and including truncating `settings` — never reach for `--force` to get past a confirmation whose actual effect you haven't read; (2) scoping it with `tablesFilter` in `drizzle.config.ts` to work around that prompt only filters the *current-DB* side of the introspection diff, not the desired-schema side, so it concluded every other table was "missing" and generated a bogus CREATE-TABLE-everything plan (caught before any damage only because it happened to hit `organizations` first and errored out). If `generate`+`migrate` ever needs a comparable non-interactive escape hatch, don't assume one exists without reading the actual CLI behavior first.
- **Platform-operator access is `PLATFORM_ADMIN_EMAILS`, never a role or a database column.** `requirePlatformAdmin()` 404s (not 403) to stay undiscoverable. An empty list grants nobody.
- Suspending an organization takes effect on its next request (`tenantFromOrgId` refuses a non-Active org) and deletes nothing — contrast with the real, destructive delete above.
- **`src/proxy.ts` must let self-authenticating API routes through** (`/api/cron/*`, `/api/whatsapp/send-reminders`) — Vercel Cron calls them with an `Authorization` header and no cookie; those routes check `CRON_SECRET` or an Admin session themselves.
- **`forEachActiveOrganization()` (`src/lib/platform/runner.ts`) is still sequential**, but the *original* forcing reason (every org sharing one Google service account's Sheets-API rate limit) is gone post-migration — kept sequential now for simplicity and easy per-org error isolation, not necessity. Revisit if a future cron job needs to run far more often or against many more orgs.
- **Adding a new key to `MODULE_ACCESS` does not reach anyone already signed in** — grants are baked into the JWT at login, so a session minted before the grant existed returns 403 for it, Admins included, until they log in again.
- **An FMS step's Ledger Movement Action is not best-effort — its chaining (`emitFmsEvent`) and its WhatsApp notify list (`notifyStepComplete`) both are.** A bad SKU or insufficient stock in an Action must throw and leave the step Pending, since the Action is the point of the step. A broken downstream flow, or a WhatsApp send failing (no phone on file, ChatXFlow not configured, network error), must never undo or block something already saved — don't make these match each other "for consistency," they're deliberately asymmetric.
- **FMS templates are never edited in place** — `updateFmsTemplate()` always mints a new `Template_ID` and archives the old one, so a running instance never sees its steps change underneath it mid-flow.
- **A production plan's FG stock has exactly one writer at a time** — either `completePlan()` (no FMS Line tracking the plan) or the Line's own final-step Action (`hasFmsLine()` returns true). Never let both fire for the same plan.
- **Every signed-in page renders inside `src/components/app-shell.tsx`**, which builds its nav from the viewer's own grants plus (new) their entitled FMS templates. Pages should not hand-roll their own nav.
- Mobile has not been assessed yet — see "Next up" above.
- **Before/after any change**: `npx tsc --noEmit`, `npm run lint`, `npm run build`, and `npm run i18n:check` should all stay clean. The last one matters more than it used to — several FMS features shipped this cycle with real UI strings that had no English translation; `i18n:check`'s "t() calls with no EN" count catches this immediately and should be 0 before calling a UI change done.
- Live-testing convention established this migration: a throwaway `scripts/*.ts` file (loads `.env.local` via `dotenv`, seeds a real org/user through `createOrganization`/`createUser`/`runWithTenant`, exercises the real code path against the actual Neon database, cleans up its own rows afterward via `deleteOrganization()`) is the standard way to verify a backend change actually works, not just that it type-checks. A few of these were left in `scripts/` as reusable examples (`verify-db-schema.ts`, `fms-live-test.ts`, `reports-shares-live-test.ts`) — most were deleted after their one-time use, which is also fine; they're disposable by design.
- **Any code sending a WhatsApp number to an external API must go through `normalizePhone()` in `src/lib/chatxflow.ts`, never read `users.phone` raw.** ChatXFlow silently drops a message with no country code — this failed with no error surfaced anywhere, for both Task-completion confirmations and FMS step-complete notify, until traced back to this one root cause.
- **A leave's Task/FMS reassignment only ever reverts what is *still* Pending and *still* held by the buddy** (`src/lib/leave/reassignment.ts`) — never blind-reverts everything the leave ever touched. This is deliberate: if the buddy already finished something during the leave, or a third party reassigned it again in the meantime, reverting it anyway would either resurrect finished work or steal it back from wherever it legitimately is now. Keep this asymmetry if this logic is ever touched.
- **`reassignment.ts` deliberately does not import from `leaves.ts`** — it reads `leaves` rows directly via `findById` to avoid a circular import (`leaves.ts` calls into `reassignment.ts` to activate/revert). If a future change wants to add a helper shared by both, put it in a third file rather than picking one side to import from the other.
- **The Holiday List and a shift's weekly-off are two separate, additive concepts — don't merge them.** Weekly-off (e.g. Sunday) is set per-org in FMS Shifts and repeats every week; the Holiday List is one-off extra dates (festivals, etc.) on top of it. Both are checked by the Recurring Task generator and by FMS's working-hours calendar, but they're stored and edited in different places on purpose — a UI that tried to unify them would stop an Admin from opening one specific Sunday without disabling the whole weekly-off rule (a real, if rare, need — see the "Week-off Override" mechanism in FMS Shifts).
- **Purchase FMS is a hardcoded 4-screen flow, not a generic FMS Template** — resist the urge to "clean this up" into the generic Template builder unless the builder actually grows a slot for vendor-first PO bundling and price snapshotting; forcing it into the generic model today would be a regression, not a simplification.
- **The General Ledger's `getBalanceSheet()` (`src/lib/accounts/ledger.ts`) adds a synthetic "Retained Earnings (Current)" equity line, live-computed as net profit to date — this is not a real account, not postable to, and exists purely so Assets = Liabilities+Equity holds without a period-close mechanism.** Don't "fix" this by trying to post it as a real journal entry; the day this codebase gets an actual period-close/closing-entry process, that's what replaces this plug, deliberately, not a patch on top of it.
- **A `tsx`-run live-test script that renders a real `@react-pdf/renderer` document will throw `ERR_PACKAGE_PATH_NOT_EXPORTED` on `@react-pdf/hyphenate`** — confirmed independently by two different sessions the same week (building PO PDF and Payslip PDF). This is a `tsx`/CJS-transpilation quirk, not a real bug — the actual Next.js production build resolves the same dependency tree fine. Don't spend time debugging a PDF-generation feature over this specific failure mode when it only shows up under `tsx`; verify the PDF pipeline via `npm run build` (or a standalone `.mts` probe) instead of trusting a `scripts/*.ts` live-test's own PDF-rendering step in isolation.
- **`src/proxy.ts`'s matcher must also exempt any new unauthenticated static asset** (PWA manifest/service-worker/icons, following `favicon.ico`'s own precedent) — forgetting this 307-redirects the asset to `/login` for anyone not already signed in, which silently breaks the exact first-time-visitor scenario (e.g. "Add to Home Screen") the asset exists for. Caught for `manifest.json`/`sw.js`/`offline.html`/icons on 2026-09-22; the same class of bug will recur for any future public, no-session asset added outside `_next/static`.
- **A one-shot financial write (Additional Payments, Petty Cash, Credit Notes, Debit Notes, Manual Journal Entry) posts its own domain row(s) AND its journal entry in one `db.batch()` — it never calls `postJournalEntry()`.** `postJournalEntry()` runs its own separate, independently-committing `db.batch()` internally, which would let the domain row and the GL posting half-happen relative to each other. This is deliberately different from `issueInvoice()`/`recordPayment()`'s own best-effort-*after*-the-fact posting (correct there, since the real document was already saved before the GL write is even attempted) — don't "fix" one to match the other, they're solving different problems. `expenses.ts`/`pettyCash.ts`/`creditNotes.ts`/`debitNotes.ts` each document this exact reasoning in their own header comment.
- **`DropdownMenuLabel` (`src/components/ui/dropdown-menu.tsx`) must always be wrapped in a `DropdownMenuGroup`** — it renders Base UI's `Menu.GroupLabel` directly, which throws an uncaught error (crashing the whole page, no boundary catches it) without a `Menu.Group` ancestor. `settings-menu.tsx` gets this right everywhere; `changelog-menu.tsx` didn't, and it crashed the Bell icon for every user until fixed 2026-09-24. Check this specifically whenever a new dropdown menu with a label is added.
- **A custom Radix/Base-UI-style popup component (`SelectContent`, `DropdownMenuContent`, ...) needs its own `collisionBoundary`/`collisionPadding` pass-through** — the library's own default (`collisionBoundary: 'clipping-ancestors'`) breaks the same way inside any `overflow-x-auto` ancestor (a scrolled table, the horizontally-scrolling nav bar). Two real instances of this exact bug have been found and fixed (the nav dropdown, then a `Select` inside the Platform org list's table) — if a third popup component gets added, give it this same pass-through from the start rather than waiting for a third bug report.
- **A new type-to-search popover picker should be built on `useComboboxNav()` (`src/components/ui/combobox.tsx`) from the start, not a fresh copy of the Input+absolutely-positioned-`<ul>` pattern.** The hook owns Arrow Up/Down/Enter/Escape and hands back `activeIndex`/`optionRefs`/`onKeyDown` for the caller's own list markup to wire `role="combobox"`/`aria-activedescendant`/`role="option"`/`aria-selected` onto — see `vendor-picker.tsx`/`item-picker.tsx`/FMS's `LookupCombobox` for the exact wiring shape. It deliberately does NOT reset its highlighted index via a ref-comparison-during-render or inside a `useEffect` — this project's lint config (`react-hooks/refs`, `react-hooks/set-state-in-effect`) rejects both of the usual React patterns for that — instead it clamps `Math.min(rawIndex, itemCount - 1)` at read time, which is simpler anyway. `AutocompleteInput` and `CustomerPicker`'s own always-open inline list are a different, simpler pattern (no popover to manage) and don't need this hook.
- **A new icon-only button doesn't need any extra work for touch targets** — `button.tsx`'s `icon`/`icon-xs`/`icon-sm`/`icon-lg` variants already carry a `pointer-coarse:` (touch-device-only, a mouse never matches) invisible `::before` hit-area sized to reach ~44px total, so just using one of these `size=` variants is enough. If a future icon button sits inside a container with less than the size's own inset worth of padding on a side (see `button.tsx`'s own comment for each variant's exact inset), check that the expanded hit-area doesn't reach into a neighboring control before assuming it's safe — the existing insets were sized specifically against `TableCell`'s `p-2` padding for the one dense-table pattern already in the app.
